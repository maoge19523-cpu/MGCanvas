---
title: Pending Tests
description: Implemented changes that still need manual verification
---

# Pending Tests

The current release needs manual verification in these areas:




- Built-in speech scripts: with a 百炼 channel (model `qwen-tts`) or a 智谱 channel (model `cogtts`) an audio generation must work without writing any script, because the built-in one is matched by the interface address. 百炼 returns a temporary audio URL and must still play; 智谱 returns raw audio bytes and must not be mangled by text decoding. The 音色 field must reach the request, and generating audio for 方舟 must fail with a clear message rather than silence.

- Segment fades and track loop: a segment's own fade in/out must darken and silence that segment's start and end, its own audio included, while a transition on the same seam still plays. Turning 循环 on for the music track must make a track shorter than the film repeat until the film ends, and its fade-out must still be measured from the film's end.

- Composite timeline: the strip under the composite panel must size each clip by its real duration, mark a segment with a transition as a diamond and a hard cut as a bar, and jump back to the source clip on the canvas when clicked. Dragging a clip body sideways must reorder the segments and the composed film must follow that new order; dragging either edge must trim that clip's in or out point in proportion to the clip's width, without crossing the opposite bound; the existing 上移/下移 buttons and 入/出 fields must keep editing the same settings. Timeline dragging now commits on release instead of on every pointer move: while dragging a clip body the strip does not reorder live, and while dragging an edge the seconds do not change live — both must apply once the pointer is released, and dragging must no longer crash the interface.

- Style gallery covers: in the canvas left panel open 提示词库, click any style and use 「设为封面」 next to 复制提示词 to pick a local image for that style; the card must show it immediately and keep it after restarting the client. Generating an image right after picking a style must also set that style's cover automatically (a manually chosen cover wins). Only built-in styles expose the button.

- Built-in style gallery: the prompt library must list a built-in 风格馆 source with 94 original style descriptions under eight categories (真人写真、东方古风、现代影像、科幻幻想、自然氛围、暗黑废墟、巨物传说、二次元插画). Filtering by category, searching, and picking a card must fill the prompt box. It must work with no network access, and it must still appear for users who already had prompt sources saved before this build.

- ComfyUI live node progress: running a ComfyUI workflow node on the canvas must show the node currently executing plus its step percentage in the node header (for example 「运行中 · KSampler · 60%」), and the node name must change as execution advances. When the run finishes the progress must give way to the usual 成功 result and elapsed time. If progress never appears but the run still completes, the work is unaffected — report it so the WebSocket path can be checked.

- ComfyUI run history: on the 「ComfyUI 本地」page a 运行历史 section must appear under the workflow library. Running a ComfyUI workflow node on the canvas must add one record with its outcome, start time, duration and outputs (images shown as thumbnails); a failed run must show its error text; 删除 must drop one record and 清空 must drop all of them; records must survive restarting the client.

- Timeline drag crash: dragging a clip body sideways or either trim edge must never crash the interface on the packaged desktop build. If the interface does show the error card, it must show the raw React message, the component stack, and the console output captured before the crash; 重试 and 重新加载界面 must both recover.

- Bailian image-to-video: giving the video node a first-frame image must produce a clip that follows that frame, while a plain text run keeps the verified text-to-video shape. Both request shapes are already verified against the live API (the image-to-video task reached SUCCEEDED and returned a `video_url`), so what is left to confirm is the in-app path from a canvas image into the video node.

- 302.AI channel: it is reachable only through the machine's system proxy (direct connections to its overseas node time out), so adding a channel from the new `https://api.302.ai/v1` preset and pressing 「获取模型」 must come back with either the model list or the provider's own wording — a network timeout instead would mean the desktop HTTP layer does not follow the Windows proxy setting and needs its own handling. Chat and image generation through that channel must work without any call script, and video or audio must be checked against the provider's real shapes before any script is written for them.

- Bailian video sizes: the video call script must send one of the sizes the service accepts (1080*1920, 1920*1080, 1440*1440, 1632*1248, 1248*1632, 480*832, 832*480, 624*624) — the earlier values made every task fail with `InvalidParameter`, verified live, while `1920*1080` polled to SUCCEEDED with a `video_url`. Generating a video through a Bailian channel must therefore return a playable clip for each canvas ratio.

- Zhipu image sizes per model: the image call script must pick canvas sizes by model, because `glm-image` only accepts side lengths divisible by 32 — the CogView size for 16:9 (`1440x720`) is rejected with 1214 (verified live), while `1728x960` succeeds. Selecting `glm-image` as the channel image model must therefore still produce an image for every canvas ratio offered in the panel.

- Channel text endpoint: text generation through a channel model from the node prompt panel, the Config-node content panel, and node retry must post to `{base}/chat/completions` and stream the answer into the node as it arrives. Zhipu answers 404 on `/responses` (verified with a real key) and 200 on `/chat/completions`; a first chunk carrying only `role` must be skipped, and a `reasoning_content` field — which both Zhipu and DeepSeek return alongside the answer — must never leak into the node text. A provider that ignores streaming and answers with one JSON body must still fill the node, and the reasoning setting must be sent as the standard `reasoning_effort` field rather than the Responses-API `reasoning` object.

- Channel video body: a channel model without a matching call script must post the plain OpenAI videos shape — `model`, `prompt`, `seconds`, `size`, `input_reference[]` — with no `resolution_name` or `preset`; a strict compatible provider must accept the request, and the resolution chosen in the panel must still arrive as `size`.

- AI director whole-film hand-off: finishing 「一键生成整片」 must leave a composition node already wired underneath the generated film, so music and whole-film trimming need no manual node and connection, and must then show a review prompt repeating the requested duration and aspect plus how many spec-check problems the generation had.

- AI director whole-film packing density: the system prompt sent to the reasoning model must carry the concrete shot count for the requested duration — 10 seconds 2–3 shots, 12 seconds 3–4, 15 seconds 3–5 — together with the duration and aspect ratio, and must tell the model not to ask the user how many shots to use. The same figures must be used by the spec self-check, so a 15-second reply cut into only two shots must raise a density warning while a three-shot reply must not, and the warning must never fire for Seedance, whose shot blocks are counted inside its own single prompt.

- Compare node divider: with two images connected, moving the pointer onto the divider band must move the split left and right without holding any modifier key, and pressing the left button anywhere on the image must drag it too — in the node and in the fullscreen view alike. The handle pill on the divider must be grabbable rather than a painted decoration. Opening the fullscreen compare and clicking the ✕ must close it, and no other antd modal opened from inside the canvas may have its close button swallowed by canvas panning.

- Canvas thumbnail cache: an image node must render a downscaled webp instead of the full-resolution source while the node occupies at most 1024 device pixels on screen, and must fall back to the original as soon as zooming pushes it past that. Opening a canvas with roughly 50 large images must feel clearly faster on the second visit than on the first, because the thumbnails come back from IndexedDB instead of being re-encoded. Thumbnails must never leak into downloads, exports, saved collages, or anything sent to a model. The cache must stay bounded: entries untouched for 30 days are dropped, and the store is trimmed back to 400 entries / 80 MB in least-recently-used order. A remote image that taints the canvas must silently fall back to the original rather than showing a broken image.

- ComfyUI local runtime summary: once ComfyUI is running, the environment panel must show the local GPU with its free and total VRAM, plus a count of locally installed models grouped as Checkpoints, diffusion models, text encoders, VAE, and LoRA. Both values come from ComfyUI's own `system_stats` and `object_info`, so they must stay empty while ComfyUI is not running and refresh after pressing the refresh button. A machine with no model files must show `No model files detected` instead of a row of zeros.

- ComfyUI startup progress: the progress bar must advance through checking the environment, starting the process, loading custom nodes and models, and waiting for the web UI, and must not reach the top of the bar while custom nodes are still loading. The loading stage must carry the hint that a first start or a node-heavy install makes this step the slowest. Connecting to a cloud ComfyUI must label the action as disconnecting rather than stopping, since no local process is involved.

- ComfyUI local workflow dependency diagnosis: the library header must read `N / M workflows available`, and every unavailable workflow must name both the missing node `class_type`s and the missing model files. Importing an API workflow must list missing models beside missing `class_type`s. A workflow whose checkpoint, LoRA, or VAE is absent from the running ComfyUI must count as unavailable in the library and on its canvas node, while an ordinary enum such as a sampler or scheduler must never be reported as a missing model.

- Beta build channel: `npm run desktop:build:beta` must produce a `猫歌映画 测试版` NSIS installer that uses the separate `com.mgcanvas.app.beta` identifier and the separate `mgcanvas-beta.exe` binary name, so installing or upgrading the beta never closes a running release build. It must install beside the release build and start with an empty workspace so release projects and settings are untouched. The beta main window title must read `猫歌映画 测试版`; the release `npm run desktop:build` must keep its identifier, its `mgcanvas.exe` name, and its `猫歌映画` title unchanged.

- Video composition node (desktop only): compose multiple connected video segments with optional background music via a user-provided local FFmpeg (auto-detect plus a manual path in Settings); verify per-segment in/out points, volumes, fades, resolution, mixed audio, and the generated video result node.
- ComfyUI 整合包引导：主 README 与模块 README 应展示ComfyUI 纯净整合包链接及「无模型、包含启动器」说明；使用整合包导入 API Format JSON、暴露输入输出并添加到画布的流程待实际环境验收。
- GitHub 展示：README 产品截图应正常加载，包含桌面项目管理、多素材创作全景、深色与浅色示例画布及 API 接入页；示例节点与实际创作截图的说明应清晰区分。

- 存储清理：仅使用 MGCanvas 数据库与存储键，验证画布状态、图片及媒体的保存、刷新恢复和删除；不再自动读取或迁移旧项目数据。

- 文档品牌清理：README 末尾仅保留一句二次开发来源说明，CLA 项目名称及中英文文档链接统一指向 MGCanvas；LICENSE 文件保持不变。
- API 注册引导：设置弹窗及独立设置页应显示整行「注册 通用服务商 · 获取 API Key」主按钮与两步操作说明；检查深浅主题、窄窗口以及桌面系统浏览器跳转，已有密钥的保存和余额验证保持可用。README 顶部应直接提供注册、连接画布与二次开发引导；验收无需触发付费生成。
- Canvas Wireless Mode: the left-side Canvas Appearance panel should expose explicit Connection and Wireless choices, with no duplicate mode control in the bottom zoom toolbar. Wireless mode should hide handles and lines without deleting existing data. Set and Unset controls must only render and execute in Wireless mode; Connection mode hides both the action and Set status indicator without clearing persisted Set data. Successful generated or uploaded image, video, audio, and text nodes must remain private until the user selects Set; native generators and exposed ComfyUI inputs can then Get them wirelessly through the node-anchored result picker. Search, type filtering, thumbnails, ordering, individual removal, Unset cleanup, source-deletion cleanup, undo/redo, restart recovery, and switching back to connection mode must remain correct. Regenerating a source should update what the next downstream run reads but must never auto-trigger paid work.
- Smart node alignment: dragging one node or a multi-node selection near another visible node's left/center/right or top/center/bottom axes should show viewport-spanning horizontal or vertical guides and apply a light magnetic snap. The perceived snap range must stay stable across zoom levels; guides must disappear after release, cancellation, or window blur, and smart alignment should take priority when grid snapping is also enabled.
- ComfyUI Local Mode: verify native selection, Python fallback, background start/stop, exit cleanup, logs, and restart recovery with Windows portable, source venv, and macOS venv installs. ComfyUI must bind only a random `127.0.0.1` port, never open its browser, and reject unauthorized profile IDs or dangerous extra arguments. Importing API-format JSON should run the five-step wizard against the live `/object_info`, allow explicit naming and selection of inputs/outputs, save missing-dependency drafts, and create a canvas macro with its complete snapshot, defaults, and named text/image/video/audio ports. Reverse dragging, compatibility, occupied inputs, output routing, and legacy handles must remain correct. Real execution should upload local inputs, call `/prompt`, expose running/stop state, parse `/history`, and fill reusable native image, video, audio, or text result nodes.
- The Windows desktop window should use the custom MGCanvas title bar integrated with the canvas, including drag regions, double-click maximize, minimize, maximize/restore, close, keyboard focus, a clear red close-hover state, and live light/dark theme sync without a white startup flash. macOS should retain its native traffic-light controls.
- The Tauri 2 desktop client should show the lightweight MGCanvas splash animation first, reveal the real canvas after local project hydration, and still reveal the main window after the native timeout if frontend startup fails. Windows file drag-and-drop, native Save As, hash-based deep navigation, Generic calls, and AppLocalData media playback must work without a browser or Vite server.
- `npm run desktop:package` should offer Windows, macOS, and all targets. Windows must create an NSIS `.exe`; macOS must create a universal Intel/Apple Silicon `.app` and `.dmg`; all must dispatch the two native GitHub Actions runners rather than claiming to build a DMG on Windows.
- Opening `/` should create or open the most recent MGCanvas project and enter the real canvas without showing the former marketing homepage; `/image` and `/video` and their Agent workbench tools should no longer exist.
- The canvas background should show subtle drifting light, a breathing grid, and pointer response without reducing node readability or changing pan, zoom, selection, drag-and-drop, or reduced-motion behavior.
- Image and video nodes should offer upload and generation from the empty state, show connected references in the attached composer, and infer text/reference generation automatically. The image footer should follow provider/model → ratio/resolution → output count → price/submit, with no More or advanced-parameter entry. Image geometry must be preset-only: no free-form ratio, width, or height input. Seedream/Dola presets must atomically derive model-specific width and height without also submitting the overriding `resolution`; the verified Generic GPT image ratios and 1k/2k/4k rules must remain model-aware; editable output counts must follow each documented model limit; fixed-count models must split 1–4x into independent single-result tasks without sending an undocumented count in any API request; and Midjourney must expose only ratio, speed, HD, and its independent 1–40 count mapping. References should disconnect only their selected edge, and completed media should fit its natural aspect ratio without blank bands or stretching.
- Generic Image G-2 must stay pinned to the documented `1k` contract and the three explicitly exemplified ratios. It must neither show nor submit the quality, `2k`, or `4k` fields found only on RHTV's direct model, and model migration must remove those stale fields.
- Node downloads must enter the browser's download list directly without opening a page or tab. Stable disk-cache URLs should be read directly; historical public HTTPS URLs should first be backfilled to `data/media-cache`, then fall back to the IndexedDB Blob behind a valid `storageKey`, direct browser reading, and finally the controlled same-origin download relay. Data or Blob URLs must be converted into a Blob before saving. A damaged or disabled IndexedDB copy must never block a healthy disk URL; 404s, empty or expired files, non-HTTPS relay targets, and local/private-network targets must show an error without navigating.
- A fresh profile should contain no preset provider endpoint, channel, model, prompt source, WebDAV directory, documentation URL, plugin registry, or remote release URL; all related screens should remain usable while empty.
- MGCanvas branding, `mgcanvas` storage/export markers, MGCanvas Agent/MCP names, and local plugin SDK identifiers should remain internally consistent after refresh, import/export, and local Agent startup.
- Async-task channel and task-node coverage: 55 documented operations, the verified 123 media models plus Kimi/Whisper/Suno, exact connected-resource and task-index placeholders, typed locally persisted result nodes, multipart proxy uploads, immediate Task-ID recovery journal, partial batch success, Midjourney `MODAL`, and stop/resume polling semantics.
- Paid Generic generation remains a manual acceptance item. Automated checks use official fixtures and the two free live action registries only; run minimum real image, video, Seed Audio, Kimi, Whisper, Midjourney, and Suno calls only after a test key and explicit cost authorization are provided.
- English and Simplified Chinese switching across navigation, settings, titles, descriptions, Ant Design components, and persisted preferences.
- Global Dropdown, Menu, Select, Cascader, and TreeSelect popup backgrounds, hover states, and selected states in both light and dark themes.
- Canvas node resize stability, prompt scrolling, generated-prompt restoration, image editing, drag-and-drop references, and generation configuration.
- Ordinary canvas nodes must stay within the 220×160 to 1600×1200 resize bounds. Aspect-locked media must never exceed the maximum bounds and must preserve its ratio when an extreme aspect ratio cannot satisfy both minimum axes. The opposite-corner anchor must remain fixed. Model, ratio, count, settings, and @-reference popups must remain attached to their node while the node moves or resizes and while the canvas pans or zooms.
- The image ratio popup should match the verified RHTV structure: a tall Auto tile on the left, a four-column ratio grid on the right, and equal-width resolution segments below, without changing each model's supported presets.
- Crop, split, and local upscale must work for both uploaded images and AI results backed only by a remote CDN URL. The editor should resolve a local Blob first, use the guarded same-origin relay when browser CORS blocks the source, and release temporary object URLs on both success and failure.
- Connections touching a running or polling generation node should show a restrained directional flow, return to normal at every terminal state, and become a static highlight under reduced-motion preferences.
- Successful Generic image, video, audio, and file outputs must be written to the repository's `data/media-cache` directory before the node receives its stable same-origin URL. With the remote CDN unavailable, refresh, crop, split, download, and downstream-reference flows must continue to work. Disk-cache failures must not be described as successful disk persistence or imply a browser copy when both layers failed; media files plus video Range serving must survive a development-server restart.
- When the first validated public IP for a CDN host refuses the connection and a later validated IP succeeds, both disk caching and the legacy download relay must fail over without weakening TLS, SNI, redirect, DNS-rebinding, or SSRF checks.
- Canvas Agent startup, MCP status, model and permission controls, approvals, version reporting, diagnostics, streaming responses, history consistency, multi-tab isolation, and local Skill management.
- Agent header tabs in English and Chinese at different panel widths; labels should collapse to icons and counts before either edge is clipped.
- Agent message layout, Markdown, code blocks, attachments, token statistics, progress timelines, command groups, and scroll-follow behavior.
- Prompt Center layout, custom-source synchronization, search, asset insertion, and prompt detail dialogs.
- Settings import/export from an initially empty configuration, Volcengine Ark protocol handling without an injected endpoint, and WebDAV-related configuration.

The Chinese version contains the complete item-by-item acceptance checklist.

- Canvas toolbar: hovering any tool icon should show a rotating and breathing rainbow ring while the icon stays legible.
- Top-of-window icons (download folder, language, back, Agent): tooltips should pop downward and stay fully visible instead of being clipped by the window edge.

## 本地示例工作流与错误提示

- 本地内置示例工作流已更换为 Z-Image Turbo 基础文生图（与本地 ComfyUI 默认工作流一致），需重新点一次「安装示例工作流」覆盖旧示例。
- 运行工作流报「环境与工作流关联环境不一致」的问题已修复，更换过 ComfyUI 目录的工作流可直接运行。
- 模型不存在时（HTTP 400）错误提示已中文化：会指出哪个节点缺少哪个模型，并列出环境里可用的模型。
- 本地与云端的「刷新状态」现在会报告真实结果：本地提示 ComfyUI 是否启动成功（含地址），云端提示连接是否正常。
- 本地工作流「添加到画布」需在本机 ComfyUI 启动成功后才可点击。
- 参数面板已暴露模型类参数（模型 / LoRA / VAE / 文本编码器），模型缺失时可直接在面板里换成环境里已有的模型。
- 重新安装示例工作流时会自动清理同名与历史版本的旧示例，避免列表里残留引用旧模型的示例而被误点。
- 旧版内置示例「示例：基础文生图」会在本机环境就绪时自动就地升级为 Z-Image Turbo 版本（保留原工作流 ID，画布上已引用的节点同时生效）。
- 工作流参数面板不再显示大模型 / VAE / 文本编码器 / LoRA / ControlNet 等技术参数。
- 图片比例换算修复：以最后手动编辑的那一边为锚点，只调整另一边（例：宽度 768 点 16:9 得到 768×432）。
- 「ComfyUI 本地」页的本地工作流区恢复「安装示例工作流」入口，并统一走示例清单（此前残留实现仍指向已废弃的示例文件路径）。
- 本地与云端的示例工作流已分开：本地页只显示本地工作流，云端页只显示云端工作流；首页不再展示示例入口。
- 修复首页/云端示例入口消失：本地环境现在也能被正确识别（此前只识别云端，导致首页示例计数为 0 而隐藏入口）。
- 画布视觉：ComfyUI 工作流节点运行时不显示转圈，进度统一在结果节点以彩虹流动进度环呈现，节点内部增加动态光晕。
- 首页三步创作卡片高度统一（此前内容长短不一导致参差）。
- 云端页状态区排版整理（标签在上、值在下，长地址截断显示）。
- 修复云端页在未连接云端时会装成本地示例：示例安装现在按调用方场景区分，云端未连接时提示「请先填写云端地址并点击连接」。
- 新增 3 个云端示例工作流（短剧资产生成器 / Krea2 角色三视图 / MiniMax H3 全能参考），云端页「安装示例工作流」改为按分类的选择弹窗，安装后直接打开新画布。
- 参数识别扩展：分辨率选择器（TTResolutionSelector / ResolutionSelector）与参数占位节点（PrimitiveFloat 等的时长）现在可在参数面板调整；标签取自节点标题。
- 工作流参数里的媒体类参数（图片/视频/音频）现在可以直接「上传本地文件」，上传后文件名写回该参数，不再只是一个无法操作的提示文字。
- MiniMax H3 工作流扩展为 6 张参考图（图片一~图片六）与 3 个参考音频（音频一~音频三，节点上限为 3）。
- H3 参数新增分辨率档位下拉（480p / 720p / 1080p），画面比例只保留 16:9 与 9:16。
- 已上传的图片/音频/视频素材在参数面板内直接预览：图片看缩略图、音频可试听、视频可播放。
- 编辑类工作流只保留一个正向与一个负向提示词（排除 system_prompt），且正向在上、负向在下。
- 未选择参考音频时对应引用会在运行时自动剔除，避免空值导致失败。
- 参数面板的素材参数新增「清除素材」按钮：清空后该参考素材会在运行时自动剔除（相当于绕过），未使用的参考图/音频不再强制保留。
- H3 示例的图片/音频槽位改名为「图片1~图片6」「音频1~音频3」，图片4~6 默认留空。
- 分辨率档位与画面比例等 ComfyUI 枚举值改为中文显示（480P 标清 / 720P 高清 / 1080P 全高清、16:9 横屏、9:16 竖屏等）；修正 megapixels 显示为英文字段名的问题。
- 修复工作流「停止」后一直卡在停止、无法重新运行：停止现在立即恢复界面与运行状态，同时后端等待结果循环会收到取消信号提前退出（此前无取消信号，最长会轮询 12 小时）。
- 修复工作流画布上出现多余的调试结果节点：调试/可视化输出（如采样曲线 SigmasGraph）不再作为结果节点，存在落盘输出（SaveImage / VHS_VideoCombine 等）时优先使用它们。
- 修复 VHS_VideoCombine 的视频输出被当成普通文件、只显示文件名路径的问题，现在按视频处理可正常播放。
- 工作流节点底部新增运行耗时显示（例：用时 1 分 23 秒）。
- 修复运行耗时不显示：成功时未保留 startedAt，导致无法计算耗时。
- 新增：单击已上传的素材图片可放大查看原尺寸，再点图片或空白处收起。
- 修复参考图被误剔除导致生成结果不参考上传素材：剔除空素材时读错了字段名（LoadImage 用 image，此前只判断 audio/file，导致所有参考图引用被删除）。
- 修复运行耗时仍不显示：成功时读取的是运行前的节点快照，现在直接传入本次运行的开始时间。
- 运行耗时显示位置调整到节点头部状态徽章旁，更醒目。
- 生成节点不再预选模型：未主动选择时显示「请选择模型」，费用预估改为「选择模型后显示预估费用」，且必须先选模型才能运行。
- 修复切换到非 Seedream 模型后无法运行：渠道模型不走参数规范化，残留的 metadata.width/height/output_format 会触发「仅适用于 Seedream 模型」校验而禁用运行按钮；现在切换与加载时都会自动清理。
- 在输入框 / 文本域上右键时显示文本编辑菜单（剪切、复制、粘贴、全选），此前只有节点菜单、无法粘贴。
- 原生生成节点（文生图等）运行完成后在头部显示用时。
- 修复右键输入框不出现文本编辑菜单：改用光标位置的元素判定（右键不一定让输入框获得焦点）。
- 生成进度环不再停在 0%：上报值为 0 视为未上报，原生节点按任务提交时间持续推进进度。
- 运行耗时：部分成功（partial）也算本次运行结束，会记录完成时间。
- 运行按钮旁常驻停止按钮：运行中可随时停止去修改参数。
- 修复渠道模型尺寸失效（回归）：此前的 Seedream 字段清理会把渠道模型用于传尺寸的 metadata.width/height 一起删掉；改为只对内置模型清理，并让校验放过渠道模型。
- 停止按钮改为按节点实际状态显示（此前只依据面板标志，渠道模型运行时按钮不出现）；节点菜单也提供「粘贴到输入框」。
- 失败与部分成功都会记录完成时间，运行耗时不再缺失。
- 内置百炼图像脚本改为「每次请求 n=1、按数量发起多次请求」，修复生成数量调到 2 时报 HTTP 400（parameters.n 上限为 1）。
- 命中「parameters.n 上限为 1」这类错误时补充可操作提示。
- 文本 / 音频 / 视频节点与图片节点一致：模型下拉默认显示「请选择模型」，不预选；未选择也可运行（按默认模型），选择后才显示费用预估。
- 模型下拉改为只显示渠道模型（所有类型），内置固定目录已移除；未配置渠道模型时提示去「配置」添加。
- 渠道编辑「接口地址」新增常用地址下拉（含 9 家服务商），支持搜索与自由输入。
- 文本节点已支持渠道模型：面板内可直接对话（text.chat），生成结果落到新节点，便于保留多版本并接入下游。
- 文本节点流水线用法：文本节点连线到图片 / 视频 / 音频节点后可作为提示词来源（@Text 1）；用「编辑文字」可让模型按指令改写原文，再交给下游生成。
- 文本节点面板改为纯文本编辑（含字数统计与用法提示），不再提供模型选择与生成按钮。
- 文本节点：一句想法 + 画风分类 → 模型扩写为专业生图提示词，结果写回节点内容，可连线给下游使用。
- canvas-agent 的 API 后端（DeepSeek / 豆包）已跑通：模型能读取画布、调用 canvas_create_text_node 等工具并在画布上真实创建节点。
- 使用前提：网页必须停留在画布编辑页（不在画布页时工具会返回「当前不在画布页」）。
- 命令行用法：先用 POST /agent/api/config 配置 baseUrl / apiKey / model，再用 POST /agent/api/turn 发送任务；DeepSeek 需使用 deepseek-chat（reasoner 不支持工具调用）。
- Agent 生成任务队列：生成类工具串行执行并自动重试限流，多镜头生成不再因并发 429 全部失败。
- AI 导演：空画布创作区或左侧「+」菜单进入，选参考素材与三项设置后生成分镜提示词，点「落到画布」生成文本节点。
- AI 导演导演台：参考图缩略图可点击放大、参考音频可内嵌试听；创意需求框输入 @ 可列出并引用已添加的参考素材。
- AI 导演：参考图带序号角标、可拖拽排序，删除按钮常显；候选图点击加入；候选音频可直接试听；镜头数改由规范自动决定。
- AI 导演参考图下发修复：本地参考图会缩放成 base64 再送给模型，不再因为本地地址报 HTTP 400。
- AI 导演：生成时显示阶段说明 + 已等待秒数 + 进度条；候选素材可隐藏并可恢复；候选区新增「先候选、点 ⊕ 才启用」的说明。

- 火山方舟语音合成：在渠道里用方舟配置生成一段语音，应当能听到声音（此前会报「方舟语音合成没有返回音频数据」；若渠道里存放过自定义脚本，请先清掉，自定义脚本会覆盖内置脚本）。

## 渠道与画布能力（本轮）

- 渠道请求地址候选探测：在渠道里填「不带 /v1 的 Base URL」或「版本段在路径中间的地址」（例如智谱 /api/paas/v4），应当能自动拼对并成功调用；填错的候选不会反复重试，认证失败会立即报错。
- 渠道服务商预设：新建渠道时点开接口地址下拉，应看到按用途分组的 19 条预设（对话与通用 / 语音合成 / 图像与视频 / 本地部署）；选中后接口地址、调用协议、常用模型应自动填好，已有模型与 API Key 不被覆盖。
- 语音渠道脚本回落：用火山方舟语音 / 智谱语音 / 百炼语音的地址配置渠道后，语音生成应命中对应的内置脚本，而不是一律走默认接口。
- 画布 `@素材名` 引用：在图片或视频节点提示词里输入 `@`，候选里应出现「我的素材」中的图片素材；写 `@素材名` 后手动点生成，该素材的图应作为参考图传给模型。超出当前模型参考图名额时，按钮附近的提示会说明有几张被采纳、几张超出上限。
- 画布缺图生成队列：画布顶部的「缺图生成」按钮应统计出所有还没有出图的图片节点，点击后串行生成；生成中再点一次可停止后续（已生成结果保留）；有失败时可只重试失败项；关掉画布重开后不应重复生成已成功的节点；若某节点未配置模型导致队列提前停止，按钮与提示会说明停在第几步。

## 我的画布项目卡片（本轮）

Status wording as above（「真机已验证」= the user confirmed it by hand in the real desktop client, independent of how well the feature is covered by automation; automation is split into「unit tests」,「rendered-output readback」and「none」）.

- **"My Canvas" project card action buttons made visible（真机已验证：用户手工确认）**: the download / rename / delete buttons in a card's top-right corner used to be **fully transparent by default (`opacity-0`) and appear only on hover**, with icons at sixty-percent white over a dark cover — on a pure-white cover the contrast was only about **3.7:1** ⇒ users simply did not know the card had those actions. The「01」node-count badge in the bottom-left corner was worse: **white text with no backing, 1.0:1 on a pure-white cover (effectively invisible)**. Now the buttons are **always visible by default** with their own dark frosted backing (`bg-stone-950/75` + `text-white/85` icons), so readability no longer depends on the cover content; all three buttons got a Tooltip (Chinese「下载画布 / 重命名 / 删除」) and the native `title` was removed to avoid a duplicate tooltip; the delete button shows the danger colour on hover and focus while **the existing second-confirmation dialog is kept**; the focus ring is guaranteed visible; the badge got a backing as well. The open arrow in the bottom-right corner was checked and **has no contrast problem** (solid white backing + near-black icon ≈19:1) and was **left unchanged — do not "fix" it again**. Result: worst-case cover button contrast **3.7:1 → 7.21:1**, badge **1.0:1 → 6.54:1**. Automation: **rendered-output readback only**, 4 cases (`web/src/components/canvas/canvas-project-card.test.tsx`) asserting the buttons **render by default** (order and count of `data-canvas-project-action`, plus `aria-label`) and **asserting that `opacity-0` and `group-hover:opacity-100` are absent**. **No automation coverage at all** for: how sharp and how good the buttons actually look in a real browser / WebView over bright covers, dark illustrations, busy photos or video frames; the real look in the light and dark themes; real hover and Tab-focus behaviour; how `backdrop-blur` renders in the WebView; the real click paths behind download / rename / delete; and the Tooltip layer's position and delay. **真机已验证：用户手工确认（2026-09-25）** — **由用户整体确认，未逐条记录子步骤**.

## 独立剪辑台与合成面板（本轮）

状态口径：**已验证**指有人在真实界面里点过并确认；**未验证**指只有代码或自动化测试，没有任何人在真实界面点过；**真机已验证**指由用户在真实桌面客户端里手工操作确认通过，与该功能的自动化覆盖情况无关，一个功能可以「真机已验证」同时「完全没有自动化覆盖」。自动化一栏区分「单元测试」（纯函数 / store 层）、「产物读回」（`renderToStaticMarkup` 静态渲染后读回 HTML 断言）与「无」。剪辑台交互项另有一份可照着点的清单：`docs/content/docs/progress/edit-bench-manual-checklist.md`。

- **变量模板撤回，只保留变量填写（已验证）**：内置风格馆里新增的 5 条「可填空模板」已按反馈全部撤回，`web/src/services/api/builtin-prompt-styles.ts` 逐字节回到加入模板之前，配套测试文件删除。`{{变量}}` 填写能力保留：在提示词详情点「复制提示词」，或从提示词库选中一条带 `{{}}` 的提示词，都会先弹出填写框，**未填写的变量原样保留**。自动化：变量提取与替换有单元测试（`web/src/components/prompts/prompt-variables.test.ts`）；「风格馆里不再出现可填空模板这一类目」没有自动化断言。
- **API 模式下 Skill 生效 + 新增「Agent 后端」开关（真机已验证：用户手工确认（2026-09-25））**：此前 API 模式（DeepSeek / 豆包）的 system prompt 只拼 Agent 指令与本地记忆、完全不注入 Skill，而前端只要 Agent 侧配了 API 模型就自动走 API 模式、界面上没有切换入口，于是装好的 Skill 在 API 模式下全部不生效。现在每轮注入 Skill 目录（名称 + 用途，总上限 6000 字），命中时只注入被界面选中或用 `$skill-name` 点名的那个 Skill 正文（上限 16000 字），目录读不到时降级为无 Skill 并写日志。The verification points as originally written: switch「Agent 后端」to Codex and to API in 连接设置 and send one message in each mode — the selected Skill must take effect in both; after choosing a backend by hand, reconnecting must not switch it back to API automatically.

  **真机已验证：用户手工确认（2026-09-25）— this round exercised one path only:「点选技能 + API 后端」.** What the user did: in the Agent panel's「连接设置」they confirmed the「Agent 后端」switch is **on（API 后端）**, with the page showing「已连接」and the local address `http://127.0.0.1:17371`; back on the chat page they **click-selected the Skill「剧本落画布」（`screenplay-writer`）**, then sent this script line:「把这段剧本放到画布上：雨夜，老陈在面馆等一个不会再回来的人。第1场 面馆内 夜。老陈擦着桌子，门开了，风把灯吹得直晃。」What the user actually observed — every criterion hit:

  - **only one text node was created** on the canvas, titled「雨夜面馆（剧本）」, its content structured as **梗概 + 人物小传 + 分场正文**;
  - the reply **contained the words「AI 导演」** and gave **5 numbered steps**（open the AI 导演 panel → paste 第1场's body and scene heading → pick the target model MiniMax H3 or Seedance 2.0 → fill in duration and aspect ratio → have the panel produce the shot prompts and drop them back onto the canvas, then repeat for the next scene）;
  - the reply **stated explicitly** that「剧本正文里我特意没有写镜头编号、H3 字段名（`integrated_multimodal_description` 之类）或警示行——这些要由导演面板按目标模型自己生成，预告会让面板产物判成混合语法而报错」;
  - the reply **quoted「你的默认是 16:9」**（which comes from the local memory entry「画幅默认 16:9，字幕用思源黑体」）;
  - the reply **volunteered that it had not touched the ComfyUI workflow node** already on the canvas;
  - **none** of `[Shot 1]`, `=== 镜头 1 ===` or `integrated_multimodal_description` appeared on the canvas, and it did **not** create a pile of one-node-per-shot nodes.

  **What is still unverified — the column this ledger exists for:**

  - **the `$skill-name` path was not tested**: this round used the interface's「点选技能」, so **naming the Skill in the message**（`$screenplay-writer`）has never been exercised by anyone;
  - **Codex backend behaviour was not tested**: the original verification points asked for one message in each of Codex / API, and only the API backend was used;
  - **「a hand-picked backend is not switched back automatically」was not tested**: keeping Codex selected by hand and reconnecting to see whether the switch stays on Codex was not tried;
  - **the log was not looked at**: the user **did not check** the `API agent skill injected` line（with `activeSkill`, `instructionsChars`, `directoryChars`）, so there is no measured figure for how many characters were injected; everything above rests on behaviour-side observation only;
  - **this is still a one-off manual confirmation with no automated coverage whatsoever**: the fix's commit touched only `canvas-agent` and two frontend components and added no tests, so **a manual pass is not regression protection** — if the per-turn injection, the caps, the backend switch or the directory-read fallback are broken later, no test will catch it.

  **Real problems found in this round's investigation（the first one has since been fixed — see the「Agent backend（本轮）」section at the end of this file; the other two are still unfixed and unverified）:**

  - **the Skill directory injection has a cap and the overflow is dropped silently**: the directory is capped at about **6000 characters**, and of the **50 enabled Skills measured, 9 never make it into the directory and are dropped silently**（including `universal-short-drama-screenwriter`, `wizard`, `skill-creator`, `skill-installer` and every system Skill）; the directory measures about **6073 characters / 12,547 bytes** and is **injected again on every turn**（the API backend is non-streaming and allows up to 24 tool-loop rounds）; the interface tells the user nothing about the dropped Skills. **Fixed in `78f15a1` — the directory now keeps only name + one-line purpose, is deduplicated by name, and a partial list says how many were left out; the figures just quoted are the pre-fix measurements and are kept here as the comparison baseline（see the「Agent backend（本轮）」section at the end of this file）.**
  - **the Claude backend injects no Skills at all**: its turn endpoint assembles the memory only; the web client has no call site for it, however, so **the interface cannot reach that backend** and no user-reachable path is affected today.
  - **API mode injects a copy of the prompt that lives in the repository**: editing the workspace's `AGENTS.md` therefore **has no effect in API mode**（Codex mode reads the workspace copy）.

  Automation: **none**（the commit touched only `canvas-agent` and two frontend components and added no tests）. **Follow-up: the directory-compression commit `78f15a1` added 10 unit tests for the directory assembly and its cap; everything else on this item — the per-turn injection, the backend switch, the directory-read fallback and the `$skill-name` path — still has no test at all.**

- **`AGENTS.md` 补充运行时数据文件规则（文档条目）**：新增「修改 `.mgcanvas` 等隐藏目录下的运行时 JSON / SKILL.md 前必须先完整读回、先备份、改完再读回逐字核对」以及「grep 默认跳过隐藏目录，搜不到不等于不存在」两条执行规则。这是约束文档，不需要界面验收。
- **独立剪辑台（真机已验证：用户手工确认）**：侧边栏「提示词库」下方新增「剪辑台」入口，有**自己的项目与素材库**，不依赖画布即可导入素材、编排时间线、编辑入出点 / 音量 / 淡入淡出 / 转场 / 字幕并导出成片。已验证的部分：**导出成片成功**（用户已确认）。其余部分——四区布局（素材 / 预览 / 时间线 / 属性）的实际观感、项目列表的新建 / 重命名 / 删除、素材三条来源的真实点击路径——**真机已验证：用户手工确认**（用户于 2026-09-25 一次性确认该项通过，未逐条记录子步骤）。自动化：四区结构、项目列表、项目不存在时的提示有产物读回测试（`web/src/pages/editor/editor.test.tsx`）；真实界面观感与真实点击链路没有自动化覆盖。
- **剪辑台素材的三条来源（真机已验证：用户手工确认）**：①「导入本地文件」用页面内的 `accept="video/*,audio/*"` 多选文件输入，导入后应探测到真实时长；②「从我的资产选择」只列 `kind === "video"` 的资产，选中即导入并现探一次时长；③ 画布视频 / 音频节点工具条上的「送剪辑」把节点产物登记为剪辑台素材，提示条里的「打开剪辑台」跳转后应带上该素材，**画布上的节点与连线不得有任何改动**。自动化：`canvasNodeToEditMedia()` 的纯函数行为、工具条按钮是否渲染、发送后节点对象逐字未变、发送落进项目，有产物读回 + 单元测试（`web/src/pages/editor/send-to-editor.test.tsx`）；**真实点击链路（三处按钮 → 系统文件选择器 / 资产弹窗 / 工具条）完全没有覆盖**。用户已于 2026-09-25 一次性确认该项通过，未逐条记录子步骤。
- **剪辑台空状态四类（真机已验证：用户手工确认）**：没有剪辑项目、项目没有素材、时间线为空、素材缺时长，四类各自的中文文案与引导按钮是否正确可点。自动化：四类文案与按钮有产物读回测试（`editor.test.tsx` 的 `editor-empty-timeline` / `editor-empty-media` / `editor-media-no-duration` 三个用例）；**按钮点下去之后的真实行为没有覆盖**（例如「把素材全部加入时间线」在没有任何探测到时长的视频时只弹提示）。用户已于 2026-09-25 一次性确认该项通过，未逐条记录子步骤。
- **剪辑台播放主时钟与漂移校正（真机已验证：用户手工确认）**：主时钟以 `AudioContext.currentTime` 为唯一时间基准（被自动播放策略挂起时退化为 `performance.now()/1000`，且时钟源只解析一次），各段 `<video>.currentTime` 只是被对齐的对象：落后超过一帧就丢帧追赶（硬 seek 有 300ms 冷却），超前超过一帧就补帧等待；段序与段尾完全由主时钟推导，不再让各段视频各播各的。自动化：主时钟推进 / 暂停恢复 / seek 重基准、漂移动作判定、冷却、期望媒体时间换算，全部是单元测试（`web/src/lib/edit/playback-clock.test.ts`）；**真机长片是否越播越偏没有任何自动化覆盖**；用户已于 2026-09-25 在 Windows 桌面版手工确认通过，未逐条记录子步骤。
- **剪辑台吸附与导引线（真机已验证：用户手工确认）**：候选点优先级为相邻片段边缘 > 播放头 > 可见网格（同优先级再看距离），阈值按 40 像素随当前缩放换算成秒；拖动速度超过 900 像素/秒自动抑制吸附，按住 `Alt` 临时关闭，按 `N` 或点时间线上的磁铁按钮全局开关；吸附位置以**竖线导引线**提示，位置只由 `paintGuide` 直写 DOM。自动化：阈值换算、候选点收集、优先级、阈值外不吸附、快拖抑制、换序落点，均为单元测试（`web/src/lib/edit/timeline-edit.test.ts`）；导引线容器与初始隐藏状态有产物读回断言（`editor-timeline-ui.test.tsx`）；**导引线是否真的画出来、吸没吸住只能手工看**。
- **剪辑台拆分 / 删除 / 涟漪删除 / 快捷键表（真机已验证：快捷键一项，用户手工确认）**：`S` 在播放头处拆分（按真实入点 / 出点切开，保留音量、淡入淡出、转场与字幕）、`Delete` 删除（后续片段前移、接缝转场保留）、`Shift+Delete` 涟漪删除（同时清掉指向被删片段的转场、播放头跟着前移）、`N` 吸附开关、`←/→` 逐帧、`Shift+←/→` ±1 秒。焦点在输入框 / 文本域 / 下拉 / 可编辑元素内时一律放行按键（否则打不出字），输入法合成中的按键也放行。自动化：快捷键表与匹配、`Backspace` 等同 `Delete`、输入框放行，是单元测试（`web/src/lib/edit/shortcuts.test.ts`）；拆分与两种删除的纯函数是单元测试（`timeline-edit.test.ts`、`web/src/stores/use-edit-store.test.ts`）；时间线角落的常驻快捷键提示与帮助弹层入口有产物读回断言；**真实按键路径（尤其焦点在输入框里打字时）完全没有自动化覆盖**；用户已于 2026-09-25 在 Windows 桌面版手工确认快捷键一项通过，未逐条记录子步骤。
- **剪辑台撤销与重做（真机已验证：用户手工确认）**：一次拖拽只记一条记录；属性区连续敲同一个字段在 600ms 时间窗内合并成一条；撤销 / 重做按钮的禁用态随操作翻转，Tooltip 显示具体操作名（形如「撤销：拆分片段」）；历史只存数据切片引用、上限 100 条，**且不落盘**（刷新页面后撤销栈为空属预期行为）。自动化：合并规则、上限、撤销重做出栈入栈、禁用态与提示文案取栈顶，是单元测试（`web/src/lib/edit/history.test.ts`）；按钮渲染出禁用态与中文无障碍标签有产物读回断言；**真实拖拽后按钮是否翻转、Tooltip 是否真的显示操作名没有覆盖**。用户已于 2026-09-25 一次性确认该项通过，未逐条记录子步骤。
- **浅色主题主按钮白底白字（已验证）**：此前 `html:not(.dark) .td-workspace-action` 的玻璃化规则以更高特异度加 `!important` 冲掉了 `.is-primary` 的底色，白色文字无人覆盖，浅色主题下主按钮对比度 1.00:1（完全看不见）。现改为主题变量并让玻璃规则排除主按钮，实测浅色 17.18:1、深色 14.23:1，普通按钮两主题无回归；同时修正了剪辑台错误提示、画布节点错误面板在浅色下的对比度。用户已确认。自动化：**无**（颜色对比度没有测试断言）。
- **剪辑台导出必失败（已验证）**：拼接成片整体淡入淡出滤镜串时在输出标签前多一个逗号，FFmpeg 把逗号与 `[vout]` 之间解析成空滤镜名，直接报 `Filter not found` / `No such filter` 并拒绝出片；该链只在整体淡入或整体淡出大于零时生成，而剪辑台默认输出恰为 0.5 秒，因此新剪辑台首次导出必然踩中（画布合成的该参数默认为空，所以此前从未暴露）。现将该段抽为纯函数从结构上杜绝尾逗号，空值与未知转场统一归一到硬切并二次兜底，同时把 FFmpeg 的大负数退出码翻译成可读说明。用户已确认导出成功；另有 Rust 单元测试（`web/src-tauri/src/ffmpeg_compose.rs` 的 `global_fade_chain` / `describe_exit_code`）与一次真实素材复现（修复前退出码大负数且无产物，修复后退出码 0 并产出 1080x620、25.1 秒成片）。
- **画布侧能力原样保留（真机已验证：用户手工确认）**：加剪辑台时明确要求保留画布上的合成节点、`compositeSettings`、合成浮动面板与 AI 导演「一键生成整片」后自动接合成节点的流程；剪辑台是并列的第二条剪辑路径，不应改动或替换其中任何一条。自动化：`web/src/pages/editor/editor.test.tsx` 只断言侧边栏入口的位置与图标，**画布侧没有任何回归测试**，只能手工确认。用户已于 2026-09-25 整体确认该项通过（由用户一次性确认，未逐条记录子步骤）；**其中「成片回落成新视频节点」与「AI 导演成片后自动接合成节点」这两个流程性子项未单独确认**——用户只是整体表示画布侧没问题，没有单独说明这两条流程是否走过，仍需要一次针对性的手工确认。
- **画布合成面板的时间标尺 / 播放头 / 顺序连播预览（真机已验证：用户手工确认）**：面板里新增按片段顺序连播的预览：点标尺或拖播放头定位、播放 / 暂停按钮（无障碍名称为「顺序连播预览」/「暂停顺序预览」）、播放头与时间读数实时跟随。预览用浏览器直接播放各段**原始素材**，不含转场、字幕烧字与多轨混音，界面已写明「预览仅用于对时，成片效果以导出为准」。自动化：片段净时长与起点、播放头全局秒数换算、段内定位反查、按 30fps 假时钟走完整条时间轴的连播切换与末段停表，是单元测试；标尺 / 总时长 / 播放头 / 一个预览播放器渲染出来，是产物读回测试（`web/src/components/canvas/canvas-composite-panel.test.tsx`）；**真实播放（是否有声、是否卡顿、切段是否跳帧）没有覆盖**。用户已于 2026-09-25 一次性确认该项通过，未逐条记录子步骤。
- **Editor audio waveform track（真机已验证：用户手工确认）**: a new audio-track row below the timeline gives every audio track a `<canvas>` drawing its peak envelope, for audio/picture alignment (previously audio had **no visual carrier at all** on the timeline). On the Rust side a new `audio_waveform` command reuses the existing FFmpeg path probe, audio-stream probe and exit-code translation; it decodes to mono low-sample-rate PCM and reads it **as a stream**, computing as it reads, so the whole track is **never held in memory**; a half sample split across chunk boundaries is re-joined with a **carry byte**; stderr is drained on a dedicated thread so the pipe cannot deadlock; peak computation and envelope resampling are extracted into pure functions. The frontend rounds the canvas device-pixel width **up** to the nearest step in `[256,512,1024,2048,4096]` and caches the result per media item per step in memory (LRU limit 24 entries, `web/src/services/edit-waveform.ts`) — **nothing is written to local storage**; the playback and drag paths **do not subscribe to the playhead**, and the canvas repaints only when data or size changes. Degradation: **missing duration / no audio stream / decode failure / pure silence** all draw just a centre line plus a message and **do not affect the rest of the timeline**. Automation: 17 unit tests (envelope step selection, strip seconds, pixel column → sample mapping, amplitude → canvas coordinates, degradation decisions; `web/src/lib/edit/waveform.test.ts`); 5 rendered-output tests (independent audio-track row with one canvas per track, the waveform strip sharing one time axis with the ruler, the missing-duration message, the placeholder row when there are no tracks, the original four-pane structure intact; `web/src/pages/editor/editor-audio-track-ui.test.tsx`); 9 Rust pure-function unit tests (`envelope_*` / `resample_*`) plus **one real-material smoke test** that runs the full decode path over audio **generated on the spot by FFmpeg** (first 2 s silent + last 2 s full-scale 440 Hz; 256 points, 4000 ms duration, the silence/peak boundary landing exactly on points 128 / 129). **No automation coverage at all** for: waveform shape and decode time on real material (mp3 / m4a / VBR / multi-channel / embedded cover art — only a generated WAV was ever tested); performance on long audio (>10 minutes); canvas sharpness and appearance in each theme in a real browser; a real-device screenshot of pixel-level alignment; and whether an audio track really lines up from second 0 during playback (listening). If timeline zoom is added later, the「step follows zoom」wiring still needs to be added. **真机已验证：用户手工确认（2026-09-25）** — **由用户整体确认，未逐条记录子步骤**.
- **Cumulative timeline clip offset fixed（真机已验证：用户手工确认）**: the video clip row used `flex` + `gap-1`, so the gaps ate pixels out of the track width and the clip strips drifted **cumulatively** away from the ruler, playhead and waveform that are positioned as strict second-based percentages; the offset was (clip count − 1) × 4px (about 2px at 2 clips, about 36px at 10). Along the way it turned out the per-second pixel conversion used for trimming was based on the **gap-compressed strip width**, so it was slightly off to begin with. Now a new `web/src/lib/timeline-scale.ts` (`timeToPercent` / `timeToPx` / `timelinePlacements`) makes the clip **outer frame occupy exactly its time share**, with the visual divider drawn **inside** the clip (an inner layer inset by 2px); ruler ticks, playhead, snap guide, waveform strips, clip edges and the trim conversion **all use that one conversion**, with a measured error of 0. **The canvas composite panel's sequential-preview timeline had the same problem and was fixed together with it.** The「one shared effective track width」alternative was rejected: flex gaps are piecewise linear, so a single vertical line cannot be expressed by one affine mapping of the same instant. **Key detail**: **before the fix the last clip's right edge was already accurate** (the end gave the gap budget back) — what was wrong were the middle clips' edges and inner widths ⇒ **asserting only that the end lines up can never find this defect**; assertions must **compare both edges clip by clip**. Automation: 11 unit tests (`web/src/lib/timeline-scale.test.ts`) + 4 rendered-output tests (`web/src/pages/editor/editor-timeline-alignment-ui.test.tsx`) + 1 composite-panel test (`web/src/components/canvas/canvas-composite-panel.test.tsx`); the new assertions were checked to fail against the pre-fix implementation (reverting the files temporarily made all 4 rendered-output tests fail; restored byte-for-byte, they pass). **No automation coverage at all** for the real-device appearance and drag feel. **真机已验证：用户手工确认（2026-09-25）** — **由用户整体确认，未逐条记录子步骤**.
- **Audio-track scrollbar no longer eats track width（真机已验证：用户手工确认）**: the audio-track row used to scroll on its own with `overflow-y-auto`, so as soon as a scrollbar appeared at 3 or more tracks it took **6–15px** out of that row's width while the ruler, clip strips and playhead resolved against a different box ⇒ the same instant no longer landed on the same pixel. Now the audio-track row **no longer scrolls itself**; vertical scrolling moved up to the **single** container wrapping the ruler + clips + audio tracks + guide line + playhead (`scrollbar-gutter:stable`). The timeline area height and the preview / inspector layout are **unchanged**. A required add-on: once that container scrolls, `overflow-x` computes to `auto`, and the ruler's end label (`left:100% + translateX(-50%)`) would conjure a horizontal scrollbar out of nowhere ⇒ `overflow-x-hidden` plus `pr-4` provides the overhang room. **This round's trap differs from the previous one**: **percentage assertions cannot catch it** — before the fix the waveform strip was equally a full-width percentage and「the right end lines up」was equally true; what was wrong was **which pixel a given second landed on**. The assertions therefore land on **pixels and structure**, and compare **several instants (0 / 1.25 / 3 / 6 / 7.5 / 11.75 / 12 s)** rather than only the two ends. Automation: 4 unit tests on the shared locatable width (`timeline-scale.test.ts`) + 2 rendered-output tests (`editor-timeline-alignment-ui.test.tsx`: the audio-track row no longer scrolls itself, and the real rendered output at 5 tracks); the new assertions were checked to fail before the fix (restoring the two components with `git checkout HEAD --` made the new cases FAILED; restored verbatim, they pass). The repo's `AGENTS.md` gained a new standing rule (elements positioned by time on the timeline must share one locatable width). **No automation coverage at all** for: how many pixels a thin scrollbar actually takes in the real WebView2 (11px / 15px / 4px); whether `scrollbar-gutter:stable` really keeps a permanent gutter in that engine (if not, the degraded behaviour is「when the scrollbar appears all four element kinds shrink together by 11px」— still same-source, just a width jump); how the scroll area really feels with many audio tracks; and the drag feel after the change. **真机已验证：用户手工确认（2026-09-25）** — **由用户整体确认，未逐条记录子步骤**.
- **Editor paused preview frames (scrub) and the black paused preview fix（真机已验证：用户手工确认）**: while paused the preview area used to be **entirely black**, from three stacked causes: ① the preview `<video>` never received a source in JSX — only the playback path assigned one; ② the paused state neither set a source nor seeked (the locate function returned immediately when not playing); ③ entering the page or pausing **actively released the source**（`removeAttribute("src")` + `load()`）, with no wait-for-frame logic anywhere. Fixed in `482aec5`: the source is kept while paused; a frame is taken at the playhead position when **entering the page, clicking or dragging the playhead, and when playback turns into pause** (paused scrub preview); drag frames are coalesced per frame, only the newest target is kept and at most one seek is in flight, so releasing does not re-seek repeatedly and slow devices do not pile up work; `preload` changed from `metadata` to `auto` (without first-frame data no paused picture can be drawn). **A hidden problem masked by the release logic was fixed along the way**: the metadata-loaded callback used to start playback unconditionally, so pausing during load made the video play and make sound in the background. **真机已验证：用户手工确认（2026-09-25）** — the user confirmed the item as a whole (their wording「验过了 这次完美」) and **no per-step record was given**. Automation: only two things were added this time — unit tests for the `resolveEditSeek` mapping (playhead seconds → in-clip media time, `web/src/lib/edit/timeline.test.ts`: second 0, an arbitrary in-clip point, clip boundaries, end of film, a single clip, multiple clips, a non-zero source in-point) and a rendered-output assertion that the preview `<video>` exists with `preload="auto"`（`web/src/pages/editor/editor-timeline-ui.test.tsx`）; **the real WebView's first-frame paint, scrub responsiveness and stutter, multi-clip switch points, whether the end of the film shows black, and whether pausing during load really stays silent all have no automated coverage at all** — passing here rests only on this manual check and is not regression protection.
- **Audio-track mute / solo / lock, and the「添加音轨」entry（未验证：只有代码与自动化测试，没有任何人在真实界面点过）**: `EditAudioTrack` gained `muted?` / `solo?` / `locked?` and `EditClip` gained `locked?`. Rules: **mute is excluded unconditionally; if any track is soloed, every other track is excluded; mute wins over solo.** **Export side**: a muted track and a track excluded by another track's solo **never enter the mixing input at all**（rather than having their volume set to 0）, so they neither sound nor change the other tracks' level, and **the existing mixing filter chain was not touched**（that chain has a trailing-comma incident in its history）. The decision is a **single source**（`web/src/lib/edit/audio-mix.ts`: `editTrackAudibility` / `resolveAudibleTracks`）**shared by the export request builder and the track-head UI**, so the interface and the finished film cannot drift apart. The video-track lock hangs off `EditClip`: a locked clip **refuses drag / trim / split / delete and shows a notice**, and **takes no part in export**. The track head is an **absolutely-positioned overlay that occupies no layout width**（to keep every element of the timeline on one locatable width）. Along the way a `history.ts` comparison function that omitted the new fields was fixed（without it the toggles「点了不动」— the switch appeared to do nothing）. **Trade-off（decided）**: a locked audio track **can still be muted / soloed / unlocked**（unlocking is only reachable there, so disabling it would deadlock; mute and solo are monitoring state, not content edits）. **Real export run**（material generated on the spot）: both tracks audible peak ≈ **0.7275696**; one track muted = 0; everything muted = 0. Automation: **unit tests**（`web/src/lib/edit/audio-mix.test.ts` 7 cases, `web/src/lib/edit/timeline.test.ts` +5, `web/src/lib/edit/history.test.ts` +1）and **rendered-output readback**（`web/src/pages/editor/editor-track-controls-ui.test.tsx` 7 cases）— **20 new cases in total**; web vitest 611 passed; Rust `cargo test --offline --lib` 16 passed / 3 ignored. **No automation coverage at all** for: whether that track is really gone **by ear** in the finished film（only waveform peaks were measured）; the real feel of a locked clip refusing drag / trim and when the notice appears; both themes; a very narrow window; whether the track-head overlay covers the start of the waveform；and the real click path of the「添加音轨」entry. **The user did not confirm this item by hand** — the two confirmations this round are on the video-original-audio and audio-track-drag items below and cover neither mute / solo / lock nor adding a track.
- **Video clips can turn their own audio off（真机已验证：用户手工确认）**: the user reported「加载的视频应该也带静音 为什么我的没有」. Diagnosis: **the editor's video track had no track-head controls at all**（no eye icon is imported anywhere in the project, and track-head markers existed only inside the audio-track component）; **the only real gap was「a video clip's own audio cannot be muted」** — before the change the clip's own audio was wired straight into the mix with no switch at all, and setting the clip volume to 0 still fed the track into the mix. Change: `EditClip` gained an optional `muted`; **the video track got a track head**（between the ruler and the clip row, absolutely positioned so it takes no width）with a「关闭原声」toggle that acts on the whole track; the property panel got a **per-clip** switch; a clip whose audio is off is marked on the timeline. **The export side reuses the existing silence branch**（`segment_needs_silence`, which previously only meant「this material has no audio stream」）: the segment still occupies one audio input（the concat requires audio/video pairs）but its source becomes a silent source. **No new filter, no new input type, and the chain's shape and order are unchanged.** **The preview stays in sync**: a muted clip plays at volume 0, and **the volume is dropped to 0 once when switching clips** so the first instant of playback cannot be at full volume. **Not added**: video-track solo（soloing the only video track would mean muting everything — self-contradictory, so it would be a fake button）, visibility（no hidden field and no rendering path）, whole-track lock（the per-clip lock already lives in the property panel）. **Real export run**: default peak 0.70806885; original audio off = 0; **two segments with a 0.5-second transition, only the second one muted ⇒ 0.70806885 / 5500 ms**（proof that only the muted segment was silenced）; the previous round's audio-track mute smoke run still passed in the same session（0.7275696 / 0 / 0）. Automation: 10 new cases（`timeline.test.ts` +4, `web/src/stores/use-edit-store.test.ts` +1, `editor-track-controls-ui.test.tsx` +5）; web vitest 621 passed; Rust `cargo test --offline --lib` 17 passed / 4 ignored. **Still unverified**: how it looks and clicks on the real machine; **how it sounds**（only waveform peaks were read）; the track head under a very narrow window and with several audio tracks scrolling; 5.1 / VFR / material with no audio track. **真机已验证：用户手工确认 — the user's wording was「可以用了」, a whole-item confirmation with no per-step record**, so which of the two paths（the whole-track switch and the per-clip switch）was actually exercised, and whether both the track-head and the property-panel controls were clicked, **was not recorded**.
- **Video-track head moved into the clip row and aligned with the audio heads（未验证：只有代码与自动化测试，没有任何人在真实界面点过）**: the user asked「我希望这个视频轨可以移到下方视频片段的开头地方 看着更整齐一些」. Change: the head no longer owns a row of its own — it moved into the clip-row container with **its positioning class names copied verbatim from the audio-track head**, still an absolutely-positioned overlay that takes no width; the clip row's four pointer handlers were not touched. The first clip's label gets left padding to clear the head（`pl-[76px]`）. The timeline area's fixed height went **236px → 214px**（exactly the row that was freed）and that space went to the preview area（timeline `shrink-0`, preview `flex-1`）. Automation: **rendered-output readback**, 3 cases（`web/src/pages/editor/editor-timeline-alignment-ui.test.tsx`）asserting that the head and every clip live in the same row container and that clip strips are still positioned by percentage and were not squeezed narrower; **the two decisive assertions were checked to fail against the pre-change code**, while the other two only guard invariants（they pass before and after, so **they are not evidence**）; web vitest 624 passed. **No automation coverage at all** for: whether the three track heads' left edges really land on one vertical line in a real window; a very narrow window; both themes; whether `pl-[76px]` is enough（font differences）; and **the first clip's left trim handle, whose leftmost ~4px sits under the head and cannot be clicked（estimated, not verified in a real window）**. **The user did not confirm this item separately** — their「可以用了」was about turning the original audio off, not about moving the track head.
- **Audio tracks can be dragged left and right to set their start time（真机已验证：用户手工确认）**: the user asked「音轨为什么不能左右拖动？openreel 上就可以拖动音轨」. Root cause: **an audio track had no notion of a timeline position at all**（its meaning was「the whole item is mixed in from second 0」）, so there was no drop target to drag. Change: `EditAudioTrack` gained an optional `start?: number`（seconds）; the whole row is draggable（except the track-head overlay）, the waveform moves with it, and **snapping reuses the existing** `snapPointsFor` / `resolveEditSnap` / `editSnapThresholdSeconds` / `isEditSnapSuppressed` **and the existing guide line**, snapping to second 0, the playhead and video clip boundaries; the property panel also offers an exact input and a keyboard entry point; a locked track never starts a drag, shows a not-allowed cursor and gives a notice. **During the drag only a ref is written and the picture is changed directly; the commit happens once on release**（the React #185 discipline）. `history.ts`'s comparison function gained `start`（two earlier omissions of the same kind had made a control「点了不动」）. **Export side**: a new `track_chain()` appends `adelay=ms|ms` at the **end** of the chain only when `start > 0`; every existing step's order and parameters are untouched, it is a single filter, and **both FFmpeg versions support it**（8.0.1 and the 2016 build N-78313 both report no `No such filter`）. The `afade=t=out` start is now derived from the span, so the fade's absolute position still lands at the end of the film after the delay; **with a delay of 0 the chain is byte-for-byte what it was before**. **The upper bound is the finished film's total length**（reason: the mix takes the first input's length, so a start at the end of the film means the track never sounds, and the total length is exactly the ruler's 100%, so no extra constant is needed）. Dragging to the upper bound collapses the waveform strip to zero width, but the row can still be dragged back and the property panel can set an exact value. **Real export run**: default total peak 0.727 / first 0.9 s 0.7211; start at 1 s total peak 0.708 / **first 0.9 s 0.0000** / after 1.1 s 0.7075; start at 3 s 0.0000 throughout; **all three films are 3000 ms long（the total length did not change）**; the two existing smoke runs still passed. Automation: 19 new cases（`web/src/lib/edit/timeline-edit.test.ts` +6, `editor-timeline-alignment-ui.test.tsx` +7, `timeline.test.ts` +3, `waveform.test.ts` +2, `history.test.ts` +1）; web vitest 643 passed; Rust `cargo test --offline --lib` 18 passed / 5 ignored（including pure-function tests for `track_chain`: no `adelay` when the start is 0, appended only at the end of the chain for a 3-second start, and the upper bound being the film's total length）. **Still unverified**: the feel of the drag on the real machine, the feel of the snapping, a very narrow window, both themes, performance with a long audio file; and **none of it is visible in the preview**（the preview does not mix audio at all, so it can only be heard after export）. **真机已验证：用户手工确认 — the user's wording was「试过了 没问题」, a whole-item confirmation with no per-step record.**（The OpenReel track-model comparison and the「real gaps」conclusion are recorded in `docs/content/docs/progress/openreel-reference.md`.）
- **Seam transition markers on the timeline（未验证：只有代码与自动化测试，没有任何人在真实界面点过）**: a transition could previously be chosen **only** from the property panel's dropdown（7 kinds plus a hard cut）, and a seam carried **no visual at all** ⇒ adding a transition changed nothing you could see, and nothing told you where a transition could be added in the first place. **The data model did not change**（`EditClip.transition` always meant「this clip → the next one」）. Each seam between two adjacent clips now renders one marker, **centred on the boundary line, absolutely positioned, with the inline style writing only `left` and never a width**（the width follows the content）⇒ **it takes no part in track width allocation**. A seam that has a transition is a pill（one icon per kind plus a short name, e.g.「交叉溶解」）; a hard cut is a 12×16px dashed slot that is **always visible** and expands to an 18×18「＋」on hover or focus. Clicking it opens a Popover（a portal, so the popup never enters the timeline's layout）holding a transition-type Select, a duration of 0.2–1.5 s and a delete action. When the clip is `locked` the panel still opens but its controls are disabled and a lock notice is shown. **Boundaries**: with `clips.length < 2` it returns nothing（0 clips and 1 clip alike）; there is no seam before the first or after the last clip; on very short clips the marker **neither shrinks nor yields** — its position is always exactly the boundary percentage（it would rather overlap than point at the wrong place）. **Regression evidence — the strongest this round**: the clip strips' inline styles rendered **before** the change were frozen verbatim as assertions, and a diff of the **whole video-clip-row render output** before and after came back **3 clips byte-identical: true**; the row container's class name is still verbatim `relative h-12`, and the number of inline styles containing `width` in that row still equals the clip count. **Pre-change failure check**: after `git checkout --` restored `edit-stage.tsx` to HEAD, the new UI tests gave **2 failed | 6 passed**（the failures being「the chip exists and its position equals the clip boundary」and「the icon differs per kind」）. **The 9 pure-function cases are not evidence**: the module did not exist before the change, so they could not run at all. **The remaining assertions only guard invariants and boundaries**（the frozen clip-strip styles, the row container class name, ruler ticks plus playhead, the one-clip-does-not-render case, and the two panel assertions on a brand-new component unrelated to `edit-stage`）— they pass before and after, so **they cannot be used as evidence**. Numbers: tsc exit 0; vitest **661 passed | 2 skipped (663)**, **70 passed | 1 skipped files**（baseline 643/645, 68/1 ⇒ **+18 cases: 9 seam pure functions, 8 UI, 1 store**）. **No Rust change** — cargo and FFmpeg were not run. **Artifact readback**（3 clips [4,6,5] / 15 s）: the two chips' `left` values are **strictly equal** to c1's and c2's `left + width`; with 1 clip the chip count is 0. **Still unverified**: how the two themes actually look in the real client（only antd tokens are used and no colour is hard-coded, but no human has looked at it）; **real clicking and keyboard operation**（the repo has no `@testing-library/jsdom` and adding dependencies is forbidden ⇒ there is only SSR readback plus store unit tests, **no click test at all**）; whether the Popover really flips its placement at a window edge; **two chips overlapping on very short clips — a zero-length clip in particular puts both seams on the same percentage, so the chips stack completely**; how it feels when the chip covers roughly 6px of the trim handle on either side of the boundary; **the chip does not follow during a drag or a trim**（the change is committed only on release）; and the finished-film result keeps the old conclusion. **The user did not confirm this item** — this round's「没问题」was about SRT / WebVTT import（the next item）and does not cover seam transition markers. Along the way `AGENTS.md` gained one line of team convention（new elements on the timeline are always absolutely-positioned overlays that write only a left offset and must never take pixels out of the track width）; that is a one-line convention and **not part of this feature's ledger**（the file already carries it, so it is not recorded again here）.
- **Importing SRT / WebVTT subtitles（真机已验证：用户手工确认）**: subtitles used to **hang off a video clip, one sentence per clip**（`EditClip.subtitle?`）with a duration always equal to the whole clip, so they could not express the arbitrary time ranges a subtitle file contains; the user chose to do this item. **Key design decision — independent subtitle entries**: a new `EditSubtitle{id,start,end,text}` stored in `EditProject.subtitles?`, whose times are **absolute seconds on the finished-film timeline**, the same design language as last round's audio-track start time. **Why**: a real subtitle file has an entry every 2–4 seconds while clips are often tens of seconds, so one clip would receive many entries and the「one sentence per clip」model has nowhere to put them ⇒ **distributing them onto clips by time would necessarily have to merge or drop some**. Entries that cross a clip seam, fall outside every clip, or overlap each other are **all kept exactly as they are**. **What the user is told**: after an import the app states how many entries were read and how many were skipped（grouped by reason: timecodes out of order / empty text / missing time line / unreadable timecode）, and says how many fall after the end of the film（they will not appear in the export）, how many run past the end（only the first part shows）, how many cross a clip seam（they show continuously）, and how many overlap the previous entry（two lines appear at once）; when the timeline is empty there is one more line. **Nothing is dropped silently and nothing is merged silently.** The property panel lists the imported subtitles, each editable, deletable, or clearable; when there are too many it lists only the **first 50** and **says plainly that the list was truncated**. **Parser**（pure functions that never throw）: SRT / VTT auto-detection（the `WEBVTT` header）; both `,` and `.` timecodes; omitted hours; BOM; CRLF / CR / LF; missing or scrambled indices; extra blank lines; multi-line text; **a missing blank line between entries**（split on the time line）; reversed or zero-length entries（skipped and counted）; hours > 99 and minutes / seconds > 59; fewer than three millisecond digits; empty text（skipped）; VTT `WEBVTT` headers / NOTE / STYLE / REGION（not counted as skips）; cue ids and cue settings; inline markup（`<v Speaker>` / `<i>` / `<c.x>`）, inline timestamps and HTML entities（stripped）; a stable sort for out-of-order input; a 2000-entry file; empty files, garbage and binary. **Byte decoding order: UTF-8 → UTF-16 with BOM → GB18030 / GBK as the fallback.** **Export**: Rust gained `ComposeSubtitle` and a `subtitles` field（`#[serde(default)]`）, and a new pure function `collect_subtitle_lines()` **merges clip subtitles and imported subtitles into one list** written into the same temporary subtitle file. **The filter chain, the font size and the alignment were not changed by a single character**; with no subtitles the request body **does not even carry the key**, verbatim identical to before ⇒ that is an **invariant-guard** assertion. **Export really run, read back at pixel level**: black 4-second material plus two imported subtitles ⇒ **brightest pixel 246 at 1.5 s and 246 at 3.0 s; outside the subtitles 0 at 0.3 s and 0 at 3.8 s; the no-subtitle control 0 at 1.5 s**; 10853 bytes with subtitles against 9422 bytes for the control; both 4000 ms. The existing smoke runs all passed in the same session（audio start 0.727 / 0.0000 / 0.7075 at 3000 ms; original audio off 0.708 → 0 including a 0.5-second transition at 5500 ms; track muted 0.7275 → 0 / 0; waveform ok）. The only failure was `media_cache::caches_configured_public_media`（`MGCANVAS_MEDIA_SMOKE_URL is required: NotPresent`）— a **pre-existing environment blocker unrelated to this change**. Numbers: tsc exit 0; vitest **706 passed | 2 skipped (708)**, **72 passed | 1 skipped files**（baseline 661/2, 70/1 ⇒ **+45 cases, +2 files: 36 parser, 6 UI readback, 3 export request body**）; `cargo check` exit 0; `cargo test` **19 passed / 0 failed / 6 ignored**（baseline 18/0/5）. **Pre-change failure check**: on the frontend, `git stash push -- <10 source files>`（keeping the new tests and the edited `timeline.test.ts`）made **2 of the 3 new `timeline.test.ts` cases fail while all 5 UI cases failed**; `stash pop` restored them afterwards. On the Rust side, **short-circuiting the independent-subtitle loop** made the smoke fail **by pixel**（the measured brightest pixel was 0 against the control's 246）⇒ **proof that this is verified by pixels rather than by an exit code alone**. The parser is an **entirely new capability**（the function did not exist before the change, so the new tests could not be run「before the change」）, so **targeted mutations** were used instead: turning the millisecond conversion back into `Number(fraction)` made「fewer than three millisecond digits」fail, and making the `WEBVTT` header drop the whole block again made「a missing blank line after the header」fail; both were reverted. **Still unverified**: **5000+ entries end to end**（performance）; how the two themes look; **the timeline's visuals**（no element was added to the timeline this round — imported subtitles are visible only in the property panel）; how 1080p burned-in subtitles look at a large font size（the smoke run is 320×240）; the skip-reason wording and the truncated-list wording; and ⭐ **an important design trade-off: subtitles are positioned on the finished-film timeline, so an SRT written for the original material will not line up if the editor trimmed or reordered it — that is a design choice, and no alternative path that remaps subtitles per clip was provided**. **真机已验证：用户手工确认 — the user finished acceptance and replied「没问题」, and this explicitly included importing a Chinese SRT and checking it for mojibake** ⇒ **the「Chinese encodings under WebView2」item is recorded as confirmed by the user on a real machine and is no longer listed as unverified**（importing a Chinese SRT also implies the button-and-file-picker path was exercised）. **It must still be said plainly that this was a whole-item confirmation with no per-step record**, so the user's confirmation **does not cover** these sub-items: the click path itself（there is no automated click test, since the repo has no `@testing-library/jsdom` and adding dependencies is forbidden）, 5000+ entry performance, how the two themes look, and the design trade-off described above. **No automation coverage at all** for: real file picking of a subtitle file, 5000+ entry performance, both themes, the timeline visuals（nothing was added there this round）, and 1080p large-font burned-in subtitles.
- **Subtitle blocks on the timeline（未验证：只有代码与自动化测试，没有任何人在真实界面点过；⚠️ 用户从未单独确认过字幕块）**: imported subtitles（the item above）used to be visible **only in the property panel** — the timeline showed nothing at all. Now every imported subtitle renders on the timeline as a **draggable block**: **dragging the block body slides it as a whole（duration unchanged）, dragging either end changes its start or end, clicking selects it and highlights it in the property panel and scrolls that row into view, and `Delete` removes it**. The property panel still lists only the **first 50**; **a subtitle selected from outside those 50 gets an extra row of its own** so the selection cannot land on nothing. A block's **position and width share the one conversion used by the ruler, the playhead and the clip strips**（`web/src/lib/timeline-scale.ts`）. Overlapping subtitles have their overlap interval computed by that same conversion, **drawn as a hatched band that states how many entries overlap**; a **very short subtitle keeps a `min-width:6px` clickable floor**（the same precedent as the clip strips: it does not push its neighbours, and it doubles as the way to grab a block that fell outside）；a block whose start lands **after the end of the film** collapses into a **small still-grabbable block** plus a「末尾之外 N 条」summary；a block running past the end is **clipped to the end by the same rule the waveform strips use**. **The timeline sets no entry cap**（reason: cutting entries would make the timeline lie）while the property panel still lists 50. **Subtitles do not snap to each other**（reason: an imported subtitle file is already precisely timed, and with many entries the candidate points would fight over the drop and the drag would feel granular）. **The timeline was not made taller**（the row lives inside the existing scroll container; the timeline is still `h-[214px]` and the preview is still `flex-1`），and **the whole row renders nothing when there are no subtitles**. Automation: **rendered-output readback above all** — the clip strips' inline styles are verbatim equal to the literals frozen before the change; **the whole video-clip-row render output is `toBe` byte-identical between the「with subtitles」and「without subtitles」renders**；the waveform strips' styles, the ruler ticks and the playhead are verbatim identical; the shared container's class names are frozen verbatim; and「the subtitle row label contains none of `p[lr]-` / `px-` / `m[lr]-` / `mx-` / `border-l` / `overflow` / `max-h-`」while `data-edit-timeline-scroll` occurs exactly once in the file. **This is a brand-new capability, so there is no「before the change」to fail against ⇒ it was checked with 7 targeted mutations**: M1 width converted separately from duration → 3 failures; M2 width dropped → 11; M3 end clamping dropped → 6; M4 the row rendered even with no subtitles → 1; M5 `overflow-y-auto px-2` added to the row → 1; M6 same-instant ordering reversed → 1; M7 the subtitle row pushed into the video-clip row → 7（**including that byte-for-byte comparison**, which shows the diff really does detect that failure mode）。⚠️ **M6 failed only 1 case ⇒ that revealed「nothing was guarding this one」⇒ an assertion was added and confirmed to fail on its own.** Figures: tsc exit 0; vitest **748 passed | 2 skipped (750)**, **74 passed | 1 skipped files**（baseline 706/2/708, 72/1 ⇒ **+42 cases, +2 files**: 23 pure functions + 19 rendered-output readbacks）；cargo test **19 passed / 0 failed / 6 ignored**（baseline 18/0/5 — ⚠️ **that baseline includes an unrelated uncommitted Rust change that was in the working tree at the time**, stated as it is）. **仍未验证**: how the drag actually feels and how the snapping feels in the real client; how readable overlapped text is under the hatched band; both themes; whether a 6px-minimum block is really clickable in a very narrow window; the real browser performance with 500 subtitles; whether the timeline height is right in a real window; and what the property panel's `scrollIntoView` actually does. ⚠️ **The user has never confirmed the subtitle blocks on their own** — the user's subtitle-related confirmation was about the previous item（the SRT / WebVTT import）and **does not cover the subtitle blocks on the timeline**.
- **Audio tracks draw volume and fades, and the volume line can be dragged（未验证：只有代码与自动化测试，没有任何人在真实界面点过）**: an audio track previously had only its waveform — **neither the volume nor the fades were visible**. Volume is now mapped in **decibels**（**−36 dB at the bottom → +12 dB at the top, with 0 dB / 100% at 75% of the row height**），because **on a linear mapping 0.5× and 2× draw at different heights, so the user cannot read off the line how much they changed; on an equal-decibel mapping equal gains are equally tall, and the same number of pixels dragged up or down is also a comparable change in loudness.** The two kinds of element are positioned differently on purpose: **the polyline is an SVG overlay covering the whole row**（it writes no left offset of its own, and its internal coordinates go point by point through the existing conversion）；**the volume baseline is content positioned by time**（its left offset and width come from that same conversion, so both ends coincide exactly with the waveform in the same row）. Boundaries: when fade-in and fade-out together exceed the track length, **both ramps are drawn at their real length and the polyline takes the product envelope**（matching the export side, where the two fades multiply）and the overlap is marked; a single fade longer than the track **is drawn only up to the track boundary and does not change the stored value**; with a start offset everything is in **absolute finished-film seconds**; a start past the end **renders nothing at all**. Dragging **only changes the picture, commits once on release, and writes nothing when the value did not change**（so no empty undo entries）；the drop aligns to the same one step the property panel uses, with **±2px snapping to 100%** and `↑/↓` moving one step per press. A `muted` track can still be dragged（muting is a monitoring state, volume is a mixing parameter）and is distinguished by a dashed line and a faded look. Automation: the fixtures were generated by「**temporarily restoring the HEAD component → capturing the real render output → writing it back**」, **not by hand-written literals**; **all 29 targeted mutations were caught**（a single mutation breaks 1–14 cases）；⚠️ **4 of them scored 0 hits at first ⇒ that revealed nothing was guarding those paths ⇒ assertions were added**（the readout writing an old value back, the `↑` direction reversed, a locked track not refusing the drag, and **the waveform strip switching to a second conversion**）；**3 further mutations were aimed specifically at the regression freezes**（`overflow-y-auto` added to the row, the overlay inset by 1px on each side, a px container wrapped around it）→ each broke exactly 1 case, **proving the regression assertions are not tautologies**. The component hash was identical before and after the mutations and everything was restored. Figures: vitest **786 passed | 2 skipped (788)**, 76/1 files（+38 cases）；cargo **19/0/6**. **仍未验证**: how the drag actually feels（**there is no jsdom and pointer events cannot be dispatched, so there is only source-level guarding**）；the contrast of the volume line and the ramps in the dark theme; the hit band in a narrow window; whether a muted track looks muted enough; whether the ramp shape corresponds to what is heard; and the keyboard focus ring.
- **Fix: the audio-track mute button looked dead, and the volume curve could not be seen（仅间接确认：用户后续使用中未再报告，且截图中可见新开关存在；⚠️ 没有确认过图标切换）**: the user's words:「**音轨的静音键打不开了 一直在静音**」. ⭐ **The real cause**: **the muted and unmuted states rendered the very same crossed-out speaker icon**, changing only its colour and backing ⇒ after the click the look barely changed ⇒ it was reasonably read as a dead button. **The state had been toggling correctly all along.** For comparison, the video track's「关闭原声」button（`videoMuted ? VolumeX : Volume2`）and the lock button in the same row（`Lock / LockOpen`）already swapped icons by state — **mute was the only one that did not follow**. That code has been there since `fbd5e67` and is unrelated to `279f06f`（proved by back-searching the strings frozen before the change）. **The investigation also confirmed**: solo and lock click and toggle correctly; dragging the whole audio row left and right is unaffected（inside the track-head area the drag simply never starts）；and **`history.ts`'s comparison function already covers all 10 fields of an audio track**, with every field changed on its own detected as a change, `muted true→false` included ⇒ **there is no「the update was dropped」case here**（that possibility has happened twice in this repository before, hence the dedicated audit）. **A hit test in a real browser（headless Edge + the built CSS + the rendered DOM read back）**：`elementFromPoint` at the centre of each of the three buttons hits the button itself; the stacking order is track head z=10 > volume line z=3 > overlay z=2（`pointer-events:none`）⇒ **the「the overlay eats the click」hypothesis does not hold**. ⭐ **Along the way a second real defect was found in the same batch of changes**: the graphics layer carrying the curve had only「four-sided fit」and no width or height, while that element expands one-to-one with its internal coordinate system ⇒ **it was stretched into a square as wide as the audio row**（measured: 860×860 while the row was 860×36），with the curve drawn about 228px below the row and its area spread over 834px, **all of it cut away by the row's overflow ⇒ the curve simply could not be seen in the real interface**. Explicit width and height were added. **A second entry point**: the property panel's audio-track settings now carry mute and solo switches that write the same state and read the same `editTrackAudibility` decision（the property panel's mute marker and its「被其它轨独奏排除」also come from it now）. **The motive was to stop a purely visual problem of this kind from leaving the user with nothing to operate at all.** Automation: vitest **796 passed | 2 skipped (798)**, 77/1 files（+10 cases）；cargo **19/0/6**. Cross-check: after `git stash` restored the three source files, the same test file gave **5 failures and 5 passes**；⚠️ **stated plainly: the stacking part of「the overlay does not intercept clicks」was already true before the fix, so it guards against future regressions and is not the detector for this root cause.** **仍未验证**: a real mouse click cannot be automated（no jsdom, no new dependencies — only rendered-structure assertions plus the browser hit test）, so **the end-to-end click was never run**. ⚠️ **Where the real-device confirmation stops**: the user later reported「音轨的声音已打开 但是我播放还是没声音」and that same batch of feedback showed the mute / solo switches looking correct ⇒ **this can be recorded as「the appearance problem has been indirectly confirmed by the user's actual use」**, but **do not write that the user confirmed the icon swap item by item**.
- **Preview playback now mixes the audio tracks in（真机已验证：用户手工确认「有声音了」；由用户整体确认，未逐条记录子步骤）**: the editor **had no audio playback path at all** — the single audio context in the app was used only as a clock reading, **nothing was ever connected to it**, and there was no audio element either; the audio tracks fed only the waveform display, the property panel and the export request ⇒ **no matter how the user played in the editor, no audio track could be heard; the result was only discoverable after exporting**. **Why the audio context was not used to take over the media element**: locally imported material is served through a custom protocol and is **cross-origin with no CORS header ⇒ routing it through the context would simply make it silent**；native audio elements are used instead, with the element's own volume property carrying the gain. **The cost: an element's volume tops out at 1, so anything above 100% only takes effect in the finished film**（written into the interface text exactly as it is）. **The time base reuses the existing playback clock**, reading the same seconds every frame; starting, seeking and clip-data changes all **re-align by absolute instant rather than accumulating**（repeated seeking accumulates no error）；a looping track is judged against the nearest equivalent point; the drift tolerance is **3 frames**（looser than video, because correcting audio means re-decoding）. **The gain rules correspond item by item with the export-side filters**: the volume and fade ranges, fade-in measured from the start, fade-out measured against the usable length, loop modulo, and silence once past the finished film's total length. **Multiple tracks are summed without a limiter**: the export side attenuates by the input count first and then compensates, which is mathematically plain summation, so **adding a limiter would actually disagree with the finished film** ⇒ none was added, and the text says so. **The copy was rewritten to match the facts**（the old「不含多轨混音」sentence was deleted）: the short hint reads「预览含原声与音轨混音，转场和字幕以导出为准」, and the detail lists what is **included**（sequential playback of every segment with its in/out points, clip volume and fades, a clip's own audio turned off, paused-state frame capture, audio-track mixing）and what is **still not**（transitions（hard cut）, burned-in subtitles, the film-wide fade in/out, output frame scaling, volume above 100%），adding「>100% 只在成片生效」「叠加不限幅」and「成片由本机 FFmpeg 重新编码，以导出为准」. **Degradation**: material removed or undecodable → it does not take part in the mix and a hint appears, while the picture carries on; only `NotAllowedError` counts as blocked（hinted once, and pressing play again reopens the gate）, while `AbortError`（a pause interrupting an in-flight play）produces no hint. Automation: vitest **834 passed | 2 skipped (836)**, 79/1 files（baseline 796/798, 77/1 ⇒ +38 cases）；cargo **20 passed / 0 failed / 7 ignored**. **仍未验证**: **「preview actually makes a sound」cannot be automated headlessly and the implementer stated plainly that they never listened to it**；how the fades **sound**（quantified only by window levels, not sample-aligned to the start instant, with roughly 20–30 ms of AAC encoder delay — see the next item）；how the solo icon really looks and its contrast in both themes；and the real path of `loop + start > 0`（unit tested only）. ⚠️ **Recording rule**: **the user later confirmed「有声音了」** ⇒ **whether preview produces sound is recorded as user-confirmed on the real client**, but **it is an overall confirmation with no per-step record**；**the sub-items that confirmation does not cover**: whether the「>100% 只在成片生效」line was ever read, whether the three degradation hints（material removed / undecodable / blocked by the system）ever actually appeared, `loop + start > 0`, and the two items below（the fade-out anchor fix and the solo icon）.
- **Fix: the audio track's fade-out anchor（未验证：只有代码、变异核对与一次真实 FFmpeg 电平实测，没有任何人在真实界面点过）**: the old anchor was always「end of film − fadeOut」⇒ when a track has a start time and therefore occupies only part of the film, the anchor landed **after** the audio content and **the fade-out never happened at all**. It is now derived from the track's own usable length（the smaller of the material's duration and what remains after the start）；**when the start time is 0 the function returns the old expression directly and probes nothing ⇒ the output is byte-for-byte unchanged**. Only the `st` value changed in the chain — the shape, the order and the parameter count are untouched, with no trailing comma. **`fadeIn` was checked and has no problem of this kind**（`st=0` anchors the content start after removing the time offset, and it is concatenated before the delay）. When `fadeOut` exceeds the usable length the anchor is clamped to the content start, so no negative value is produced. **Real FFmpeg evidence**: segmented window levels（`atrim` + `volumedetect`, ffmpeg 8.0.1）— the old implementation gave `[4,4.9) −6.00 / [5,5.9) −6.00 / [5.9,6.0) −6.00 dB` ⇒ **exactly equal to the「no fade-out at all」control ⇒ the fade-out did not happen**；the new implementation gives `−6.00 / −10.30 / −30.80 dB` ⇒ **a drop of 4.30 / 24.80 dB, matching the linear-fade theory**. With `start=0` the old and new exported PCM are **identical by SHA-256**（sample for sample）；with `start=3` they differ. ⚠️ **A methodology trap worth recording**: window levels **must not use the output-side `-ss/-t`** — that reports silent stretches as having level and **would have given exactly the opposite conclusion**；`atrim` is required. Automation: 10 mutations（**M3 restoring the old fade-out anchor formula → 1 failure, and that assertion was added after finding nothing was guarding it**）；after restoring, the files are **byte-for-byte identical by SHA256** with nothing left behind. **仍未验证**: how the fade-out **sounds**（quantified only by window levels, not sample-aligned to the start instant, with roughly 20–30 ms of AAC encoder delay）；and whether a track with a start time fades out in a way that sounds natural in the real client. ⚠️ **The user's「有声音了」was about preview mixing as a whole and did not separately confirm this fade-out anchor fix.**
- **The solo icon now follows the state（未验证：只有产物读回，没有任何人在真实界面点过）**: `HeadphoneOff` when not soloed and `Headphones` when soloed（consistent with the「with or without a slash」expression used by mute and lock in the same row）. After the fixtures were refrozen, **a byte-for-byte check showed that the only changed bytes in the three audio-track rows all fall inside the `data-edit-track-solo` button**（the button slice went from **779 to 915 bytes**），nothing outside the button（the end of the row included）changed at all, **the video-track rows changed by 0 bytes**, and switching the old button back restores the previous fixtures byte for byte. Automation: rendered-output readback（a verbatim before/after diff of the three audio-track rows plus the byte-slice check），shipped in the same commit as the item above（vitest 834/836, cargo 20/0/7, as above）. **仍未验证**: how the solo icon really looks and its contrast in both themes；**the user did not separately confirm this icon**（their「有声音了」was about preview mixing）.
- **Fix: a track whose address cannot be resolved was skipped in silence, plus a new preview audio-track status hint（用户报告的现象在其后一次反馈中未见复现，但没有逐条确认过这两处修复本身）**: **the user's words**:「预览的时候 导入的音频还是没有声音」（the new build really was installed at that point, and the interface already showed the new copy）. **The investigation gathered real evidence**: that audio was taken out of the user's own IndexedDB（**59,995 bytes**）and verified with **the same engine**（the local Edge shares WebView2's engine）⇒ its duration matches what the library recorded, it contains **real speech throughout**（quarter-second peaks 0.3–0.64），`play()` succeeds directly, and the MP3 structure is normal（MPEG2 L3, 22050 Hz, mono, 287 frames, no Xing header）⇒ **the material itself is fine**. **The runtime log** shows the user tried `solo:false/muted:false`, `muted:true`, `muted:false` and `solo:true` in turn ⇒ **the fault is not in the mute / solo decision**；a single track with `solo:true` is judged audible, and **the export side shares that decision so it is unaffected too**（a test asserts directly that the export sends this track）. ⭐ **A real defect was fixed**: `if (!src) continue;` — when the material is still there, is not muted, but **no playable address can be obtained**, the mixing plan **skipped that track in silence**（no playback element created, no hint of any kind）, **which shows up exactly as「no sound and not a single word in the interface」**. `pending`（still resolving, stays quiet）is now distinguished from `missing`（definitely unobtainable, said outright in the interface）. ⭐ **A new audio-track status hint at the bottom of the preview area**（the whole line is hidden during normal playback）: the static half is rendered by React（material removed / no playable address / load failed），and the live half is written straight into the DOM by the playback heartbeat（should be sounding but is stopped（including the loading state and the element's volume）/ the material only reaches 0:07.4 and does not loop / the start time has not arrived yet / it is X seconds away from the playhead / it was blocked by the system）. **This makes one thing decidable**: **if the status hint never appears during playback and the sound still cannot be heard ⇒ the app side really is playing ⇒ the problem is in the system output device or the volume settings, not in the app.** **A second earlier misjudgement was also checked**: the timeline's audio waveform strip looks shorter than expected because the editor is a three-column layout and the timeline occupies only the middle column — converted proportionally it matches the expectation, **so it is not a defect**.（A separate waveform defect was found as well: when the probed duration exceeds the real decoded length the trailing buckets stay 0 ⇒ the waveform is drawn short, at most 12% in this case, which **cannot explain a discrepancy of an order of magnitude, was not reproduced and was not changed**。）Automation: vitest **846 passed | 2 skipped (848)**, 79/1 files（baseline 834/836 ⇒ +12 cases）；cargo **20/0/7**. Mutations: putting the silent skip back → 2 pure assertions plus 1 rendered-output assertion fail; making the silent judgement always empty → 5 failures. Everything was restored verbatim with nothing left behind. **Invariant guards**: state and reason are complementary; an empty parsed string falls back to the material's address; a muted track reports no gap; the status row is hidden when everything is normal. **仍未验证**: whether sound really comes out on the user's machine（needs a real audio device）；whether the runtime blob address actually resolves；and behaviour inside the Tauri app. ⚠️ **How to record the real-device confirmation**: the phenomenon the user reported **has not reappeared in the feedback that followed**（the user confirmed「有声音了」afterwards, but that confirmation was about preview mixing as a whole and **said nothing item by item about the status hint or the silent skip**）, so **it must not be inflated into「the user confirmed these two fixes work」**.
- **Fix: the audio track's volume backing covered the waveform（未验证：本轮尚未收到用户对这项修复的确认）**: **the user's words**:「音轨的调整声音的颜色太重了 把波形都遮盖完了」. **The real cause**: the backing used a **background colour derived from the primary colour** and **set no transparency**（alpha=1）；this application configures its primary colour as a **neutral**（light `#171717` / dark `#d8d8d8`），so the derived background is **not the usual pale blue but a solid mid grey `#575757`**；spread across the whole row it covered about **70.5% of the row's area**；and the waveform is drawn in the same colour family as **solid 1px columns with no gaps** ⇒ the composite is `1.0×#575757 + 0×#171717 = #575757` ⇒ **the waveform contributes 0%**. **The line the user sees inside the row is actually the volume baseline, not the waveform.** **The fix**: the backing now uses **the same primary colour as the waveform at 8% opacity** ⇒ **overlaying a colour on itself is an identity operation, so the composite over the waveform is per-pixel equal to the waveform's own colour ⇒ no theme switch can ever weaken the waveform again**；waveform visibility is 1 − 0.08 = **92%**（0% before the fix）. The backing lands only on the row's base colour: contrast is **14.2:1** in light and **9.4:1** in dark（accessibility threshold ≥3:1）. Not a single byte of the polyline or ramp coordinates changed. ⭐ **Test hardening（important）**: the backing's colour and opacity **had no assertions at all**（the old cases froze only the polyline coordinates）⇒ **which token and which opacity were used had nobody guarding them, which is exactly why the defect reached the user's screen**；four assertions now pin it down: the rendered `fill-opacity` read back, `fill` sharing the polyline's `stroke` colour, a verbatim ban on `#575757` / `#595959`, and a source-level ban on `token.colorPrimaryBg`. ⚠️ **A testing point that matters**: the render must be run once with `getAntThemeConfig(false/true)` each — **that is, with the real theme applied**；**under the default antd theme that background is the pale blue `#e6f4ff`, the problem is invisible, and only the real theme reproduces it**. Stacking re-check（measured on a composite page）: the overlay rect is exactly the row rect（the `width/height:100%` guard is kept and it never leaves its own row）；the overlay is `pointer-events-none` with no interactive child, and z-2 < volume line z-3 < track head z-10 ⇒ the centre of each of the three buttons hits itself. **The waveform strip sits below the overlay and that is deliberate**（the waveform is solid blocks, so anything drawn under it is invisible）⇒ **giving way can only be done by「the overlay not covering things itself」, never by changing the stacking order.** ⚠️ **One pre-existing limitation（not a regression from this round, not changed）**: the track-head box, about 70×26px, sits on the volume line's 12px hit band ⇒ **the volume line cannot be grabbed inside those 70px, leaving roughly 90% of the row width as the effective drag area**；raising the volume line to dodge it is not a good idea（it would cover the three switches, and a test already freezes that stacking relationship）. Automation: vitest **851 passed | 2 skipped (853)**, 79/1 files（baseline 846/848 ⇒ +5 cases）；cargo **20/0/7**. Mutations: after `git stash` rolled the component back, **3 of the new cases went red**（opacity read back as 1, the muted state 0.5 vs 0.04, and the source gate）；**the stacking and hit-band cases are only invariant guards**. **仍未验证**: whether the waveform really is clear in the real interface（no jsdom and no build this round as required, so no screenshot of the app could be taken; the browser run used **a composite page containing only the row base, the waveform blocks and the two fills — not the application UI**）；how dragging the volume line feels；and whether an 8% backing is visible enough in the light theme. ⚠️ **How to record the real-device confirmation**: the user had already confirmed「**有声音了**」before this（about preview mixing）, and **the backing fix itself has not been confirmed by the user this round**.

## Agent backend（本轮）

Status wording as above（「真机已验证」= the user confirmed it by hand in the real desktop client, independent of how well the feature is covered by automation; automation is split into「unit tests」,「rendered-output readback」and「none」）. This item is the fix for the first of the「Real problems found in this round's investigation」listed in the editor section above（**the Skill directory injection has a cap and the overflow is dropped silently**）.

- **API-mode Skill directory injection compressed（未验证：只有单元测试与一次脚本实测，没有任何人在真实界面点过）**: the problem — the API backend put **every Skill's full usage description** into the prompt on every turn and **silently dropped** whatever did not fit（measured: **9 of 50 enabled Skills never made it into the directory**）, while that directory **is sent again on every turn** of a non-streaming loop of up to **24** tool rounds, so the cost multiplied. The change: each Skill keeps only **its name + a one-line purpose**（cut at the nearest sentence boundary so that discriminating information such as「what kind of generator, for whom」survives）; when something still does not fit, the directory **explicitly says it is a partial list, how many entries are missing and how many Skills exist on this machine**, and points the user at the Skill picker to select one; entries are **deduplicated by name**; the system's prompt assembly was extracted into pure functions. Measured: the directory went from **6073 characters / 12,547 bytes to 2719 characters / 4674 bytes**（−55.2% / −62.7%）; **dropped Skills 9 → 0**; over the 24-round loop this saves up to about **80,500 characters**; the overflow fallback was measured too（with a synthetic list of 200 entries the output is 2820 characters and states that a further 162 are not listed）. **Hash equality on the same input before and after the change** proves that the memory, directory, selected-Skill body and supplementary-requirements blocks are **byte-for-byte identical in text and order**. Automation: **unit tests**, 10 cases（`canvas-agent/src/agent/api-agent.test.ts`: 0 / 1 / 50 Skills, a very long description, a very long name, everything over the cap, same-name deduplication, a selected Skill, truncation of an over-long body）; canvas-agent `npm test` 119 pass / 0 fail. **Still unverified**: whether the model **still recommends Skills on its own and whether the recommendation rate drops**（cannot be tested headlessly）; the **actual token saving**（only characters / bytes were measured）; English descriptions now cut **in the middle of a word**（cosmetic）; and **the header can read 49 while the Skill list UI shows 50 rows** after deduplication. **The user did not confirm this item by hand.**