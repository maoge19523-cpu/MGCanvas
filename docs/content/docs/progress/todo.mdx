---
title: TODO
description: Planned follow-up work
---

# TODO

- Replace the Claude Code CLI adapter with a Claude Agent SDK adapter and improve the tool queue, following the [Local Agent Integration Plan](/docs/progress/local-agent-integration-plan).
- Add Skill installation from web search, Skill resource-file management, and controlled local-memory persistence.
- Configure production Windows code signing and Apple Developer signing/notarization after the required certificates and secrets are available.
- Complete the remaining ComfyUI Local Mode phases described in the [product requirements](../../../../product/comfyui-local-mode-prd.md): isolated environment/process management, the workflow import wizard, macro-node execution, and Windows/macOS acceptance.
- Extend video composition beyond the current MVP (sequential concatenation, per-segment trimming, single background music track, fades): timeline UI, transitions, subtitles and multi-track audio.
- Extend channel-model support to text nodes: the Config-node text path and the Text-node retry still post to `/v1/responses`, which chat-completions-only relays reject, so text generation through a channel model should use the OpenAI chat protocol. Keep adding built-in call scripts for the providers users actually use.
