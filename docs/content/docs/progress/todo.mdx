---
title: TODO
description: Planned follow-up work
---

# TODO

## 风格馆：用过即封面（自动捕获）

**目标**：用户用某个风格生成出图后，那张图自动成为该风格卡片的封面，越用越好看。

**当前状态**：风格数据已内置（`web/src/services/api/builtin-prompt-styles.ts`，94 条、八类，`coverUrl` 全为空，卡片显示图标占位）；入口已放在生成节点提示词框左下角；选中风格会追加到提示词后面并保留用户已写内容。

**已定位的改动点**（下次直接照做，不必重新搜索）：

1. **记录本次用的是哪个风格**
   - `web/src/components/canvas/canvas-node-prompt-panel.tsx` 第 111 行附近，`CanvasPromptLibrary` 的 `onSelect` 现在只拼提示词；这里要顺便把风格 id 记到节点 metadata 上（需要给 `CanvasNodeData["metadata"]` 加一个字段，例如 `styleId`）。
2. **在生成成功处取图**
   - `web/src/pages/canvas/project.tsx` 的 `handleGenerateNode`（第 3279 行起）里有三到四处图片结果落点，都要处理：约第 3879 行（`...imageMetadata(uploadedImage), prompt, ...generationMetadata`）、约第 2357 行（`imagePatch` + `historyPatch`）、约第 3472 行（`imageMetadata(uploaded)`）。任一节点带着 `styleId` 成功出图时，取第一张图作为该风格封面。
3. **存封面**
   - 新建 `web/src/services/api/style-covers.ts`：用 `localforage` 存 `styleId → 压缩后的小图`。图片要先用 canvas 压到约 256px 的 webp/jpeg 再存，不要把原图或 base64 大图塞进本地存储。
4. **读取时合并**
   - `web/src/services/api/prompt-source-runtime.ts` 的内置源分支（`if (source.builtIn && !source.url.trim()) return BUILTIN_STYLE_PROMPTS;`）改成先读封面表再合并，这样卡片就能显示封面。

**说明**：只自动捕获第一张图；手动设封面的入口（卡片上直接换图）暂不做，等自动捕获跑通后再评估是否需要。已移除的历史条目（Claude Agent SDK 适配、代码签名、ComfyUI 环境隔离、内置调用脚本补充、方舟语音合成打通）都能在 Git 提交历史里找到当时的结论与线索。
